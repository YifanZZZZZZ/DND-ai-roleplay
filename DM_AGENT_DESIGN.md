# AI DM Agent 设计方案

以《蠕行深黯》(CM_04) 为检验样本。回答两个问题：建团前 DM 要输入什么；AI DM 该怎么搭。

---

## 一、先看清这个模组的真实结构

现在的实现把 `module_content` 整篇原文塞进上下文，每轮让模型自己判断"我们在哪、下一步该发生什么"。用这个模组试一次就知道为什么必然失败——它的结构完全不适合被这样对待。

**1. 地点是非线性的。** V1 杖与锤、V2 石匠作坊、V3 镇长的房子、V4 树、V5 墓园、V6 商人货车，可以任意顺序访问，也可以完全跳过。镇长屋内 H1–H11 同理。任何"第几幕"的线性模型都会在第一次玩家乱走时崩掉。

**2. 真正的主线是一条信息依赖链。** 玩家在哪个房间不重要，重要的是他们拼出了几块真相：

```
书本(起点已知) ─┬─→ F1 七十年前矿难
                └─→ F2 矿难后镇民陆续失踪
卢卡斯 ──────────→ F1 F2  ＋ 支线：洛娜的花与项链
阿斯特拉 ────────→ F2  ＋ 暗示"噩梦"是征兆
墓园石堆同日 ────→ F1
商人货车(一年前) ─→ 失踪至今仍在继续
镇长日记(H8) ────→ F2 ＋ 她自己的噩梦
妖精荒野的书(H9) →→ F3 元凶是凌弱怪  ← 全模组的枢纽线索
  ＋"这会是答案吗？"    F4 由恐惧诞生、巢穴在地下
                        F5 会把人转化成同类
苔藓隧道 DC14 ───→ F4
镇长印戒 ────────→ F6 镇长已被转化
转化室遗物堆 ────→ F5
```

H9 书房那条线索是枢纽：拿到之前，玩家在打一个不知道是什么的东西；拿到之后，整个模组的意义变了。AI DM 必须知道玩家现在在链条的哪一段——**这是"剧情进度"的正确度量，不是"第几幕"。**

**3. 有一层与地点无关的常驻压力。** 凌弱怪始终跟随、低语、制造幻影、分割队伍、长休时投放噩梦。模组给了六条具体的骚扰建议。这不是场景，是一个持续运行的背景进程，而且**用过的不能重复用**——重复三次"眼角有动静"这个模组就废了。

**4. 有必须对玩家保密的真相。**「故事之后的真相」整节是 DM-only。现在的实现把它和其余原文一起丢给 AI DM，没有任何标记说这段不能直接叙述出来。

**5. 有原文照读的 boxed text。**「当角色们进入维尔梅隆村时，对玩家阅读以下文字」——这段应该原样输出，不该被模型改写成自己的话。

**6. 有大量检定门槛。** DC13 调查暗格、DC15/DC18、进矿井 DC14 感知豁免、长休后 DC13。每一个都必须由真人 DM 投骰，AI 只能建议。

---

## 二、DM 在建团前输入什么

**核心原则：DM 只粘贴原文，结构由系统编译，但编译结果必须经 DM 确认才能开团。**

让 DM 手工填场景表是不现实的；让 AI 每轮现读原文是现在的问题。中间答案是一次性的「模组编译」。

### 必填三项

**1. 模组正文** — 粘贴或上传 `.md` / `.txt`。就是现在这个字段，但改成唯一入口（`dm_guide` 和 `module_content` 双字段并存的问题一并解决）。

**2. 切入点** — 队伍从哪里开始、已经知道什么、为什么接这个活。模组本身不会告诉你这个。本例：

> 队伍受矿业与勘探队委托调查维尔梅隆，已在烛堡查阅到《蠕行深黯》并读过全书。现在位于马林镇，刚下马车。

这一段直接决定开场草稿，也决定哪些线索一开始就算"已知"。

**3. 阵容与规模** — 已有的角色选择，加上目标时长（一次跑完 / 长期战役）。本例的「矿业股份」整节只在长期战役里有意义，短团里应当直接不进入 AI 的视野。

### 强烈建议填的三项

**4. 基调与红线** — 这个模组有"PC 被转化成凌弱怪、控制权转移给 DM"的机制，还有大量精神折磨内容。恐怖强度到哪、允不允许队友真的被转化、身体恐怖的尺度，必须事先定，不能等 AI 写出来了再喊停。

**5. 即兴授权** — 哪些部分 AI 可以自己编，哪些必须严格照模组。模组自己就区分了：通往维尔梅隆的路上「你可以放置一切你认为必要的障碍」（可即兴），而维尔梅隆的地点和线索链（不可即兴）。马林的路人商贩可即兴，卢卡斯和阿斯特拉不可。

**6. 节奏偏好** — 大约多少轮到达维尔梅隆、是否允许 AI 主动推进。默认可以留空。

### 编译流程

```
粘贴模组正文
→ 点击「编译模组」
→ AI 一次性把原文拆成结构化大纲（下一节）
→ DM 在编译结果页逐项检查、修改、补充
→ 确认后才能生成开场草稿
```

编译只跑一次，成本可以接受（这个模组 1.6 万字，一次调用就够；更长的模组分段编译）。**DM 复核这一步不能省**——编译错了后面每一轮都是错的。

---

## 三、编译产物：结构定义

以下每一项都对应新的数据表。用 CM_04 的真实内容举例。

### `locations` 地点

```yaml
- id: V3
  name: 镇长的房子
  parent: vermeillon
  read_aloud: |            # 原样保留，首次进入时逐字使用
    这栋宏伟的两层砖房和镇上其他建筑一样遭受了时间和自然的摧残…
  observable: 门户、枯树、四座摇摇欲坠的烟囱
  contains: [H1..H11]
  clues_here: [C6, C7, C9]
  threats: [悲惨转化的镇长凌弱怪]
  improvisation: restricted   # 不得新增此地没有的具名事物
```

### `facts` 真相命题 —— 剧情进度的度量单位

```yaml
- id: F3
  statement: 失踪的元凶是凌弱怪，不是诅咒
  dm_only_until_known: true
  supported_by: [C7, C8]
  significance: pivot      # 枢纽：确立后模组的意义改变
```

### `clues` 线索

```yaml
- id: C7
  source_location: H9
  content_to_players: |
    桌上摊着几本书，主题是身体和精神创伤、噩梦、神秘失踪。
    其中一本妖精荒野的书格格不入，里面夹着一张字迹歪扭的纸条：
    "这会是答案吗？"
  gate: null               # 无需检定，花时间搜索即可
  establishes: [F3, F4, F5]
```

### `gates` 检定门槛

```yaml
- id: G1
  location: V1
  skill: 调查
  dc: 13
  on_success: 发现地板暗格：铁盾(50gp)、双短剑(各25gp)、十根铁棒
  on_failure: 灰尘太厚，看不出地板有异常
- id: G3
  trigger: 在维尔梅隆长休结束
  skill: 感知豁免
  dc: 13
  on_failure: 获得一级力竭；梦见被埋在碎石下无法呼救
```

### `npcs`

```yaml
- id: lukas
  name: 卢卡斯·格罗斯文诺
  role: 无聊鼬鼠酒馆老板，95岁人类，中立善良
  traits: 富有耐心但没法忍受傻瓜；一句赞美或小费就会露出微笑
  knows: [C2]
  wants: 请人替他给洛娜的坟献花、取回树洞里的项链
  opens_thread: T1
  canonical: true          # 不可被 AI 改写或替代
```

### `ambient` 常驻压力

```yaml
- id: amb_vermeillon
  active_in: [vermeillon, V1..V6]
  rules: |
    凌弱怪保持隐秘、避开明亮光源、不与全副武装的队伍正面冲突；
    目标是分割队伍、孤立单人。
  beats:                   # 逐条消耗，不重复
    - 眼角瞥见动静，转头什么都没有
    - 耳边响起低语
    - 一声尖叫刺破寂静
    - 影子以不正常的形态从地板伸出
    - 倒影里看到亡者或仇敌的脸
    - 汗毛倒立、肩胛之间发痒，甩不掉被注视的感觉
  escalation: 队伍分散时提高频率；夜间与阴天可主动出手
```

### `threads` 支线 / `endings` 结局

```yaml
- id: T1
  name: 洛娜的花与项链
  opened_by: lukas
  steps: [在V5献花(听到风中叹息), 在V4树洞取项链]
  payoff: 归还卢卡斯
- id: E1
  condition: 巢穴被清剿且通知马林
  outcome: 维尔梅隆重建，矿山复产，队伍被视为本地英雄
- id: E2
  condition: 凌弱怪存活
  outcome: 旅行者继续失踪，巢穴壮大，最终威胁马林
```

---

## 四、运行时状态

每个 Campaign 一份，DM 可随时手动修改（AI 一定会判断错，必须有人工纠正入口）：

```yaml
current_location: H9
visited: [maerin, road, vermeillon, V5, V3, H1, H2, H8, H9]
known_clues:                     # 按角色记录，复用现有的可见性隔离
  C2: [全体]
  C6: [卡莱拉, 赛蕾妮]           # 只有这两人在场翻到了日记
  C7: []
established_facts: [F1, F2]
open_threads: [T1]
used_ambient_beats: [1, 4]
pending_gates: [G3]
pressure: 3                      # 凌弱怪的骚扰强度
```

`known_clues` 按角色存是关键——一个角色单独在书房翻到日记，其他人不该知道。这一层现有的 Recipient Snapshot 机制可以直接复用。

---

## 五、把 AI DM 拆成两个 Agent

**这是本方案最重要的一条。**

现在一个 agent 同时要：维持剧情连贯、判断进度、写出好读的叙事、决定可见范围。四件事需要的温度和推理深度完全不同，混在一起哪个都做不好，而且 DM 只能读到一段散文，无法知道 AI 到底认为剧情走到哪了。

### Narrator（叙事）

- 只写玩家会听到的那段话，1～4 句
- **不判断进度**。它收到的是已经算好的"当前场景 + 现在允许透露的线索 + 未完成目标"
- 温度较高（0.7），追求文字质量
- 每次生成 DM 草稿时调用

### Keeper（剧情管理）

- 不写任何叙事
- 只回答：现在在哪个地点、玩家刚才获得了哪条线索、哪条 fact 成立了、支线推进了没有、凌弱怪该不该行动、下一个自然的钩子是什么
- 输出纯结构化 state delta，温度 0.2
- **在 DM 发布正式消息后异步运行**，不阻塞出草稿

```
角色回复发布
   └→ Narrator 出草稿（读当前编译片段 + 状态）
        └→ DM 审核 / 编辑 / 发送
             └→ Keeper 异步跑，更新状态
                  └→ DM 在侧栏看到状态变化，可直接改
```

Keeper 的产出对 DM 是**可读、可改**的：「玩家进入 H9；获得线索 C7；F3 现在成立」比一段散文有用得多。它同时也是 AI 是否跑偏的早期警报。

---

## 六、两个 Agent 各自看什么

### Narrator Context

```
<模组权威设定>          当前地点全文 + 相邻地点摘要（不是整篇模组）
<不可直接说出的真相>    该地点相关的 DM-only 内容，明确标注"只能暗示，不能陈述"
<原文照读>              首次进入时的 boxed text，要求逐字使用
<现在允许透露>          Keeper 算出的、此刻可给的线索
<还没揭开>              相关但尚未获得的线索（供 AI 埋钩子，不得直接说破）
<常驻压力>              未使用过的 ambient beats + 当前 pressure
<最近对话>              带角色名和可见范围的最近 N 条  ← 现在这里连名字都没有
<最新角色回复>
<主持风格与红线>
```

关键差别：**不再注入整篇模组**。当前地点 + 相邻摘要通常一两千字，比现在的一万六千字少一个数量级，而且相关性高得多——现在模型要在"矿难死了30人""咸鱼惊喜3铜币""凌弱怪AC15"里自己找哪句有用。

### Keeper Context

```
<线索图>        全部 clues / facts 及依赖关系（压缩版，只有 id 和一句话）
<当前状态>      上面那份 runtime state
<自上次以来>    新发布的消息
<地点图>        相邻关系
```

---

## 七、输出 Schema

```python
class NarratorDecision(BaseModel):
    content: str = Field(max_length=800)
    audience: Literal["PUBLIC", "PRIVATE"]
    recipient_character_ids: list[str]

    # 以下字段供 DM 审计，不进入玩家可见内容
    used_read_aloud: bool          # 是否照读了原文
    reveals_clue_ids: list[str]    # 这段话透露了哪些线索
    used_ambient_beat: int | None  # 用掉了哪条骚扰
    suggested_check: Check | None  # {角色, 技能, 建议DC, 理由}——只建议，不投骰
    deviation: Literal["none", "soft", "hard"]
    deviation_note: str | None     # hard 时必填
```

`deviation` 那三个字段是给"守不住模组"这个问题的直接答案：AI 每次都要自己声明这段话是照着模组走的、还是即兴填充的、还是玩家已经跑到模组之外了。DM 在草稿上看到 `hard` 标记就知道要介入。

`suggested_check` 与 PRD §3.14 严格一致：AI 建议，真人 DM 确认参数并投骰，结果回填后 AI 只负责写结果叙事，**不得改变系统已确定的成败**（这条现在完全没有约束，需要在 prompt 里明写，并在有 DC 时把结论作为不可协商的输入）。

---

## 八、必须写进 Narrator Prompt 的硬规则

现在的 DM prompt 里关于剧情忠实度**一条都没有**。至少要有这些：

1. 模组内容 > 场景笔记 > 最近对话。三者冲突时以模组为准。
2. 不得引入模组中不存在的具名 NPC、地点或情节转折——除非当前地点标记为 `improvisation: allowed`。
3. `<不可直接说出的真相>` 里的内容只能通过环境、线索和 NPC 的片面认知暗示，绝不能由旁白直接陈述。
4. 首次进入带 `read_aloud` 的地点时，逐字使用原文，不要改写。
5. 玩家偏离模组时，优先设计自然的引导回归；确实无法回归时把 `deviation` 标为 `hard` 并说明，交给真人 DM 判断。
6. 不得自行投骰、不得替玩家决定检定结果。有 DC 的检定结论由系统给出，你只写对应的叙事。
7. 常驻压力每次最多用一条，且不得重复已用过的。

---

## 九、分期实施

**第一期（几十行改动，立刻能感觉到差别）**
- DM context 的对话历史加上角色名和可见范围（现在只有 `CHARACTER` / `DM`，模型根本不知道谁在说话）
- 建统一的 `DmContextBuilder`，四种触发类型走同一入口、按类型分化 prompt
- 把上面第八节的硬规则写进 prompt
- `MANUAL_ASSIST` 补上对话历史（现在完全没有）
- PRIVATE 草稿的 ID 非法时报错而不是静默转 PUBLIC

**第二期（本方案的核心）**
- 模组编译 + DM 复核页
- `locations` / `clues` / `facts` / `gates` / `npcs` / `ambient` / `threads` 七张表
- 运行时状态 + DM 手动修改入口
- Narrator 只读当前地点窗口，不再注入整篇模组

**第三期**
- Keeper agent 与异步状态更新
- `deviation` / `reveals_clue_ids` 审计字段与前端状态侧栏
- `SKILL_CHECK_RESULT` 触发类型（现在枚举里有，代码里没实现）

第一期不依赖第二期，可以马上做。

---

## 十、需要你拍板的

1. **编译要多细？** 我给的粒度是每个地点一条、每条线索一条。CM_04 编译出来大约 30 个地点、10 条线索、7 条 fact、十几个 gate。更细会更准但 DM 复核的负担更重。
2. **Keeper 要不要现在就做？** 它是每条正式消息一次额外调用（可以异步、可以用便宜模型）。不做的话状态得由 DM 手动维护，或者让 Narrator 兼职判断——后者会退回到现在的问题。
3. **玩家跑到模组外面时默认怎么办？** 我倾向"AI 标记 `hard` 并停下等 DM"，另一种选择是"AI 尝试即兴，但明确标注"。前者更安全，后者更流畅。
4. **要不要保留自由文本的场景笔记？** 我倾向保留，作为编译结构之外的临时补充（比如 DM 临时决定的东西），但优先级低于模组。
