# AI TRPG 网站 MVP 产品需求文档（PRD）

> 版本：v0.3  
> 文档状态：产品需求已确认，技术选型待讨论  
> 产品形态：真人 DM + 多个长期存在的独立 AI 角色 + 群聊式跑团网站  
> MVP 使用方式：单用户、本机、电脑端网页  
> 核心产品原则：真人 DM 掌握世界与规则；AI 只扮演自己的角色

---

## 0. 文档说明

本文是 AI TRPG 网站 MVP 的主产品需求文档，用于统一产品行为、数据边界、交互流程和验收标准。

本文不负责最终确定：

- 前后端框架；
- 实时通信协议；
- 数据库迁移方案；
- LLM 供应商与具体模型；
- 部署与进程管理方式；
- Excel 模板的逐单元格解析映射。

上述内容将在产品需求确认后单独形成技术设计。

本文中的角色均指 AI 扮演的玩家角色；用户始终担任唯一真人 DM。

### 0.1 已确认的最高级原则

1. 用户是唯一 DM，负责世界、NPC、敌人、规则和结果裁决。
2. 每个 AI Agent 只扮演一个长期存在的角色，不存在独立的“玩家人格”。
3. 角色属于全局角色库，不隶属于单个战役，可以经历多个 Campaign。
4. 角色的记忆、态度和性格表现会随长期经历逐渐丰富。
5. AI 可以决定角色自身的台词、意图和行动，但不能决定外部结果。
6. 第一个需要 DM 裁决的角色行为出现后，所有 AI 立即停止并等待 DM。
7. AI 不写入 HP、法术位、物品数量、状态或其他游戏数据。
8. MVP 只严格维护当前 HP；其他机械资源由 AI 结合历史尽量自我约束，DM 最终裁决。
9. Excel 只提供角色实际拥有的 D&D 身份、能力、法术和装备信息。
10. Roleplay Prompt 是角色核心人格和行为逻辑的唯一初始配置来源。
11. Agent 不获得角色逻辑上不应知道的信息。
12. 所有已发布内容自动保存，并严格归属于对应角色、Campaign 和 Session。

---

# 1. 产品概述

## 1.1 产品愿景

构建一个供真人 DM 使用的 AI 跑团网站。

用户负责描述世界、扮演 NPC、提出挑战并裁决结果；多个独立 Character Agent 分别长期扮演不同角色，通过类似群聊的界面发言、行动、讨论和建立关系。

系统的目标不是让 AI 高效完成任务，而是让角色像长期存在的冒险者一样：

- 根据自身性格、经历和认知思考；
- 自主决定是否发言；
- 主动提出计划、调查和讨论；
- 形成不同观点，但不为了冲突而冲突；
- 隐瞒、拒绝、妥协、信任或产生戒备；
- 记住自己真实经历过的事件；
- 随多个战役积累习惯、创伤、关系和目标；
- 在需要世界或规则反馈时自然将叙事权交还给 DM。

## 1.2 核心体验

1. DM 选择 1～6 名全局角色组成当前 Campaign 的队伍。
2. DM 创建或继续一个 Session，并发送第一条场内消息。
3. 能看到该消息的角色独立判断是否回应。
4. 系统每次只发布一个通过验证的完整角色气泡。
5. 新角色消息再次成为事件，角色可继续自然讨论。
6. 某条角色消息一旦需要外部结果，所有 AI 停止并等待 DM。
7. DM 给出场内裁决后，相关角色根据最新事实继续。
8. Session 结束时，系统为各角色整理独立摘要与长期记忆。
9. Campaign 完成时，系统更新角色的长期成长档案。

## 1.3 目标用户

- 希望担任 DM，但缺少稳定玩家的跑团爱好者；
- 希望测试模组、场景和角色互动的 DM；
- 喜欢长期人物塑造与关系发展的用户；
- 希望 AI 更像自主角色，而不是剧情助手的用户。

## 1.4 MVP 成功标准

MVP 需要验证：

> 以 5 名角色为主要测试规模、最多支持 6 名角色时，多个拥有独立人设、认知和长期记忆的 Character Agent，能否在真人 DM 主持下持续产生自然、有区别、重视团队合作且不越过叙事权限的跑团互动。

---

# 2. MVP 范围

## 2.1 MVP 必须包含

- 全局角色库；
- 创建和编辑长期 AI 角色；
- 角色头像上传；
- Roleplay Prompt 编辑、导入和导出；
- 固定模板 Excel 角色卡上传、解析、预览和替换；
- DM 手动设置角色最大 HP；
- Campaign 创建、筹备、进行、完成、重新开启、归档、重置和永久删除；
- 每个 Campaign 选择 1～6 名角色；
- 同一时间只允许一个 ACTIVE Campaign；
- Session 手动开始、继续和结束；
- 群聊式跑团页面；
- DM 场内消息；
- DM OOC 纠正；
- DM 向一个或多个角色发送私密消息；
- Agent 向 DM 声明少量秘密行动或台词；
- 多个角色独立判断和自主讨论；
- 发言排序、候选失效、停止条件和消息上限；
- DM 停止 AI 自动对话；
- DM 新消息优先打断未完成输出；
- DM 手动维护当前 HP；
- 角色定性健康状态；
- 独立认知、滚动摘要、长期记忆和角色成长档案；
- 页面刷新后的恢复；
- Campaign 与角色导出；
- 基础调试信息与 Token 记录；
- 电脑端网页体验。

## 2.2 MVP 暂不包含

- AI 扮演 DM；
- AI 自动控制 NPC 或敌人；
- 完整 D&D 规则引擎；
- 自动攻击、伤害、治疗、豁免或检定结算；
- 网站内骰子工具；
- 法术位、能力次数、物品消耗等严格资源追踪；
- 自动修改 Excel 或角色卡；
- AI 写入任何游戏数据；
- 战斗地图；
- 聊天图片、地图或文件附件；
- 语音跑团；
- AI 图片生成；
- 职业构筑器；
- 网页内完整角色卡编辑器；
- 多名真人玩家；
- 多账号与登录系统；
- 多租户；
- 移动端适配；
- 历史消息搜索；
- 数值化好感度或关系系统；
- 角色删除；
- ACTIVE Campaign 中的角色离队；
- Campaign 导入恢复；
- 完整角色卡版本历史；
- 多个同时进行的 Campaign；
- 后台定时触发的角色自发消息；
- 公开社区或战役分享市场。

---

# 3. 产品角色与权限

## 3.1 真人 DM

DM 拥有：

- 世界叙事权；
- NPC 与敌人控制权；
- 规则与检定裁决权；
- 新世界事实发布权；
- 公开和私密信息发布权；
- OOC 纠正权；
- 当前 HP 和最大 HP 写入权；
- AI 自动对话停止权；
- Campaign、Session 与阵容管理权；
- 角色配置、记忆和成长档案管理权；
- 数据导出、重置与删除权。

## 3.2 Character Agent

每个 Character Agent 只扮演一个角色，拥有：

- 自己的 Roleplay Prompt；
- 当前 Character Development Profile；
- 自己的长期记忆；
- 自己有权看到的旧 Campaign 摘要；
- 当前角色实际拥有的能力、法术、身份背景和装备资料；
- 自己及队友的定性健康状态；
- 当前 Campaign 中自己有权看到的消息；
- 自主沉默、发言和行动决定权；
- 在合理动机下向 DM 秘密声明行动的能力。

Character Agent 不拥有：

- 世界结果决定权；
- NPC、敌人或环境控制权；
- 其他角色行为决定权；
- DM 总览摘要访问权；
- 其他角色 Prompt、秘密或记忆访问权；
- 加入当前 Campaign 前的消息访问权；
- HP、法术位、物品、状态或任何数据库写入接口；
- 展示模型隐藏推理的权限。

---

# 4. 核心产品模型

## 4.1 实体关系

```text
Character Library
  └── Character
       ├── Roleplay Prompt
       ├── Avatar
       ├── Active Character Sheet
       ├── Max HP
       ├── Character Development Profile
       └── Character Memories

Campaign
  ├── Campaign Memberships (1～6 个角色)
  ├── Sessions
  │    ├── Messages
  │    ├── Character HP States
  │    ├── DM Summary
  │    └── Character Summaries
  └── Agent Runs
```

## 4.2 角色与战役的关系

- Character 是全局长期实体，不属于单一 Campaign。
- 一个 Character 可以参与多个 Campaign。
- 同一时间全站只允许一个 ACTIVE Campaign，因此不会出现角色同时在两个进行中战役里积累记忆。
- Character 的 Prompt、头像、Excel、最大 HP、成长档案和长期记忆跨 Campaign 保留。
- 当前 HP、消息、Session 摘要和运行状态属于具体 Campaign。
- 跑团经历生成的长期记忆记录来源 Campaign 和 Session；DM 手动添加的记忆标记为手动来源。
- 删除或重置 Campaign 时，只移除源自该 Campaign 的记忆，再重建成长档案。

## 4.3 Campaign 生命周期与归档

```typescript
type CampaignLifecycleStatus =
  | "PREPARATION"
  | "ACTIVE"
  | "COMPLETED";

interface CampaignArchiveState {
  archivedAt?: string;
}
```

- `PREPARATION`：筹备阵容和角色配置，尚未开始跑团。
- `ACTIVE`：正在进行；全站同时只能有一个。
- `COMPLETED`：DM 明确结束战役；默认只读。
- 归档不是生命周期，只负责从常用列表隐藏。
- ACTIVE Campaign 不允许归档。
- 可以同时筹备多个 PREPARATION Campaign。
- COMPLETED Campaign 在没有其他 ACTIVE Campaign 时可以重新开启。
- 结束一次 Session 不等于完成 Campaign。
- 关闭网页或长期未游玩不会改变 ACTIVE 状态。

## 4.4 核心概念数据

以下结构只用于明确产品归属关系，不代表最终数据库实现：

```typescript
interface Character {
  id: string;
  name: string;
  avatarRef?: string;
  roleplayPrompt: string;
  activeSheetRef: string;
  maxHp: number;
  developmentProfile: string;
}

interface CampaignMembership {
  campaignId: string;
  characterId: string;
  joinedAt: string;
}

interface Session {
  id: string;
  campaignId: string;
  title: string;
  status: "ACTIVE" | "ENDED";
}

interface SessionCharacterState {
  sessionId: string;
  characterId: string;
  currentHp: number;
  maxHpAtSessionTime: number;
}

interface Message {
  id: string;
  sessionId: string;
  senderType: "DM" | "CHARACTER" | "SYSTEM";
  senderId?: string;
  dmMessageType?: "IN_GAME" | "OOC";
  content: string;
  audience: "PUBLIC" | "PRIVATE" | "DM_ONLY";
  visibleToCharacterIds: string[];
  supersededByMessageId?: string;
  invalidatedByOocMessageId?: string;
  createdAt: string;
}
```

产品规则：

- Message 通过 Session 唯一归属于一个 Campaign；
- PUBLIC 消息仍保存发送时的可见角色快照；
- 角色后来加入 Campaign 时，不会被补入旧消息的可见列表；
- OOC 修订不覆盖原始记录，而是通过版本关系确定当前有效消息；
- `maxHpAtSessionTime` 用于保护历史 Session，不受之后全局最大 HP 修改影响。

---

# 5. 信息架构

## 5.1 主要页面

电脑端 MVP 包含五个主要页面：

```text
战役列表
全局角色库
战役详情 / 阵容与 Session 管理
角色详情 / Prompt / 角色卡 / 成长与记忆
跑团主页面
```

开发调试页使用独立入口，不放入主要导航。

## 5.2 战役列表页

展示：

- 战役名称；
- 可选描述；
- 生命周期状态；
- 是否归档；
- 最近游玩信息；
- 当前是否为唯一 ACTIVE Campaign。

支持：

- 创建 PREPARATION Campaign；
- 进入战役详情；
- 归档和恢复；
- 重新开启 COMPLETED Campaign；
- 导出；
- 重置；
- 永久删除。

## 5.3 全局角色库

展示：

- 头像；
- 显示名称；
- 种族、职业和子职摘要；
- 最大 HP；
- Prompt 是否配置；
- Excel 解析状态；
- 参与过的 Campaign 数量。

支持：

- 创建角色；
- 编辑角色；
- 替换 Excel；
- 查看和编辑成长档案；
- 查看、添加、编辑、删除和固定长期记忆；
- 导出角色。

MVP 不提供角色删除。

## 5.4 战役详情页

PREPARATION 状态支持：

- 编辑战役名称和描述；
- 从全局角色库选择 1～6 名角色；
- 移除尚未开始参与的角色；
- 检查角色配置完整性；
- 启动 Campaign。

ACTIVE 状态支持：

- 查看当前阵容；
- 在 AI 未运行时增加新角色，最多 6 名；
- 查看历史 Session；
- 创建或继续当前 Session；
- 结束 Campaign；
- 导出、重置或永久删除。

ACTIVE 状态暂不支持角色离队。

COMPLETED 状态只支持：

- 查看；
- 导出；
- 归档；
- 重新开启；
- 永久删除。

必须重新开启后才能修改阵容或增加 Session。

## 5.5 跑团主页面

采用两栏桌面布局：

```text
┌──────────────────┬─────────────────────────────────────┐
│ 队伍与 HP         │ Session 状态与运行控制               │
│                  ├─────────────────────────────────────┤
│ 头像              │                                     │
│ 角色名            │ 聊天消息时间线                       │
│ 当前 HP / 最大 HP │                                     │
│ HP 条             │ 私密消息、OOC 修订、等待 DM 提示     │
│                  ├─────────────────────────────────────┤
│                  │ 消息类型 / 可见范围 / 接收者          │
│                  │ 输入框                         发送   │
└──────────────────┴─────────────────────────────────────┘
```

规则：

- 左栏只展示队伍头像、名称与数值 HP；
- 输入框固定在聊天区域底部；
- 等待 DM 的具体事项固定显示在输入框上方；
- 顶部提供“停止 AI 自动对话”和 Session 操作；
- 不显示 Scene 面板；
- 不显示时间戳；
- 不支持移动端布局。

---

# 6. 全局角色管理

## 6.1 创建角色

必填：

1. 角色显示名称；
2. Roleplay Prompt；
3. 固定模板 `.xlsx` 角色卡；
4. DM 手动输入的最大 HP。

可选：

- 角色头像；未上传时使用默认头像。

约束：

- 不设置草稿角色；
- 最大 HP 必须是正整数；
- Excel 必须解析成功并由 DM 确认预览后才能创建；
- Excel 中的角色名可以自动填充，但最终以网页显示名称为准；
- 同一 Campaign 内的参与角色不能重名；
- 全局角色库中允许存在暂未参加任何 Campaign 的角色。

## 6.2 编辑角色

允许修改：

- 显示名称；
- 头像；
- Roleplay Prompt；
- 最大 HP；
- 当前 Excel；
- Character Development Profile；
- 长期记忆。

运行约束：

- AI 正在判断、生成或验证消息时禁止编辑角色；
- DM 可先停止 AI 自动对话；
- 修改从该角色下一次 Agent 调用开始生效；
- 不改写已有消息；
- 不自动清空既有记忆；
- 全局配置修改会影响该角色之后参与的所有未结束 Campaign；
- 历史消息保留发送时的名称和头像快照。

## 6.3 Roleplay Prompt

支持：

- 在线纯文本或 Markdown 编辑；
- 粘贴；
- 从 `.md` 或 `.txt` 导入；
- 单独导出为 `.md`。

文件限制：

- `.md` 或 `.txt`；
- 最大 1 MB。

MVP 不拆分性格、价值观、目标、说话方式等表单字段，也不使用富文本编辑器。

## 6.4 头像

支持：

- PNG；
- JPG / JPEG；
- WebP；
- 最大 5 MB。

上传后生成正方形缩略图，跑团页面优先加载缩略图。

## 6.5 最大 HP

- 最大 HP 由 DM 手动输入，不从 Excel 读取。
- 角色首次进入一个新 Campaign 时默认满血。
- 修改最大 HP 时，当前 ACTIVE Campaign 中该角色的当前 HP 同步为新的最大 HP。
- 替换 Excel 不影响最大 HP 或当前 HP。
- 历史 Session 的 HP 记录不因全局最大 HP 修改而改写。

---

# 7. Excel Character Sheet

## 7.1 模板定位

MVP 使用统一 Excel 模板，参考文件为：

```text
赛蕾妮_6级咒剑魔契师.xlsx
```

所有角色卡均按同一模板填写。后续技术设计需形成固定单元格与区域映射。

MVP 只接受：

- `.xlsx`；
- 最大 10 MB；
- 符合模板工作表和关键区域结构的文件。

## 7.2 Excel 的用途

Excel 用于提取角色实际拥有的：

- 种族和亚种；
- 职业和子职；
- D&D 身份背景；
- 故乡、语言和工具；
- 职业能力；
- 种族能力；
- 专长；
- 已知或已准备法术；
- 实际装备；
- 背包物品；
- 上述能力、法术和物品的完整说明。

## 7.3 明确排除的数据

Agent 不获得：

- 属性值与调整值；
- 技能值；
- 豁免值；
- 熟练加值；
- AC、DC、先攻、被动察觉；
- 攻击加值；
- 伤害骰和伤害数值表；
- 法术位数量或剩余资源；
- Excel 当前 HP 或最大 HP；
- 未选择的职业能力；
- 未学习或未准备的法术；
- 模板数据库工作表中的完整职业、种族或法术库；
- Excel 中的个性、理念、羁绊、缺陷和背景故事。

Excel 中的角色扮演信息一律不能覆盖 Roleplay Prompt。

## 7.4 能力说明与防止超游

Agent 可以读取角色实际拥有能力和法术的完整说明，以便理解自己能做什么。

但 Agent 输出必须遵守：

- 不说出属性、技能、AC、DC、加值、伤害骰、法术位等机械数值；
- 不主动申请某项技能检定；
- 不以“我的调查是 +X”等方式讨论角色卡；
- 不以规则术语替代角色行动；
- 直接描述角色想做什么，由 DM 判断是否检定和结果；
- 自然叙事中的普通数量不受禁止，例如“两个人”或“三天前”。

发布前检查器需要阻止机械表达，并要求 Agent 重新用场内语言生成。

## 7.5 上传与解析预览

创建或替换角色卡时：

1. 上传 `.xlsx`；
2. 验证类型、大小和模板结构；
3. 读取白名单区域；
4. 生成标准化角色能力快照；
5. 显示解析预览；
6. DM 确认；
7. 保存为当前有效角色卡。

预览至少展示：

- 种族、职业、子职和背景；
- 识别到的能力名称；
- 法术名称；
- 装备名称；
- 背包物品名称；
- 各类别识别数量。

## 7.6 替换规则

- 解析失败不覆盖旧角色卡；
- DM 取消确认不覆盖旧角色卡；
- 替换成功后只保留新的有效 Excel 和解析结果；
- 网站删除旧 Excel 副本和旧解析结果；
- 需要回退时由 DM 重新上传本地旧文件；
- 替换 Excel 不修改 HP、历史消息、记忆或成长档案。

---

# 8. Campaign 管理

## 8.1 Campaign 字段

```typescript
interface Campaign {
  id: string;
  name: string;
  description?: string;
  lifecycleStatus: "PREPARATION" | "ACTIVE" | "COMPLETED";
  archivedAt?: string;
  createdAt: string;
  updatedAt: string;
}
```

产品界面不显示时间，但系统内部保留时间用于排序和审计。

## 8.2 阵容

- PREPARATION 阶段从全局角色库选择 1～6 名角色。
- PREPARATION 阶段可自由调整尚未产生历史的阵容。
- ACTIVE 后仍可以新增角色，但总人数不能超过 6。
- MVP 不支持 ACTIVE 角色离队。
- 中途加入角色从加入后的第一条可见消息开始建立当前 Campaign 认知。
- 中途加入角色完全不知道加入前的公开消息、私密消息或摘要。
- 中途加入角色仍保留自己在旧 Campaign 中实际获得的长期记忆。
- 未来版本可以提高角色上限，MVP 数据模型不应把 6 写成不可迁移的永久限制。

## 8.3 启动 Campaign

启动条件：

- 当前没有其他 ACTIVE Campaign；
- 已选择 1～6 名配置完整的角色；
- 每个角色均有 Prompt、有效 Excel 和最大 HP。

启动后：

- Campaign 变为 ACTIVE；
- 尚不自动创建角色发言；
- DM 创建 Session 并发送第一条场内消息后，AI 才开始运行。

## 8.4 完成与重新开启

- 只有 DM 可以完成 Campaign。
- 完成前结束或终止当前未完成 Agent Run。
- 如果存在进行中的 Session，先结束 Session 并完成记忆整理。
- Campaign 变为 COMPLETED。
- 系统根据全部来源记忆更新参与角色的 Character Development Profile。
- COMPLETED Campaign 默认只读。
- 当前没有其他 ACTIVE Campaign 时，DM 可以重新开启。
- 重新开启后可以添加新 Session，旧 Session 保持只读。

## 8.5 归档

- PREPARATION 或 COMPLETED Campaign 可以归档。
- ACTIVE Campaign 不允许归档。
- 归档只影响列表展示，不影响数据和记忆。
- 可以从归档列表恢复。

## 8.6 重置跑团进度

重置用于 DM 对跑团结果不满意并希望从头开始。

重置时：

- 停止未完成 Agent Run；
- 删除 Campaign 的全部 Session；
- 删除全部消息；
- 删除 DM 摘要和角色独立摘要；
- 删除源自该 Campaign 的长期记忆；
- 清除该 Campaign 的运行状态；
- 重建受影响角色的 Character Development Profile；
- 保留 Campaign 名称、描述和已选角色；
- 保留全局角色 Prompt、头像、Excel 和最大 HP；
- 将 Campaign 恢复为 PREPARATION；
- 不自动创建 Session。

重置不可撤销。确认窗口提供“先导出记录”，但不强制导出。

## 8.7 永久删除 Campaign

永久删除时：

- 停止未完成 Agent Run；
- 删除 Campaign；
- 删除其 Membership；
- 删除所有 Session、消息、摘要和运行日志；
- 移除源自该 Campaign 的长期记忆；
- 重建受影响角色的 Character Development Profile；
- 保留全局角色、Prompt、头像、Excel 和最大 HP；
- 不影响角色在其他 Campaign 中的记忆。

删除不可恢复，必须输入 Campaign 名称二次确认，并提供“先导出记录”入口。

---

# 9. Session

## 9.1 定位

Session 表示一次实际游玩记录，不是游戏回合，也不是打开一次网页。

一个 ACTIVE Campaign 同时最多有一个未结束 Session。

## 9.2 创建与继续

- DM 手动创建 Session。
- 标题可选；默认使用本地日期形成名称，DM 可改名。
- 页面刷新、关闭浏览器或隔天回来不会结束 Session。
- 再次进入时默认继续未结束 Session。
- 新 Session 中各角色当前 HP 继承上一 Session 的最终 HP。
- Campaign 中第一次出现的角色默认满血。
- 开始 Session 后 AI 保持安静，必须由 DM 发送第一条场内消息。

## 9.3 结束 Session

- 只有 DM 可以结束 Session。
- 如果 AI 正在运行，先取消尚未发布的输出。
- 已完整发布的消息保留。
- 保存最终 HP。
- 生成只读 DM 总览摘要。
- 更新各角色独立 Session 摘要。
- 为各角色提取长期记忆。
- Session 变为只读，不能重新开启或追加消息。
- 下次继续 Campaign 时创建新 Session。

## 9.4 记忆生成失败

- 某个角色摘要或记忆生成失败不阻止 Session 结束。
- 界面标记失败角色。
- DM 可以稍后单独重试。
- 失败内容不写入角色记忆。

---

# 10. 消息系统

## 10.1 消息类型

DM 消息只分为：

```typescript
type DmMessageType =
  | "IN_GAME"
  | "OOC";
```

- `IN_GAME`：场景、NPC 台词、行动结果和世界事实。
- `OOC`：纠正错误信息、规则理解或无效行动，并要求相关 Agent 重新处理。

场景叙述与 NPC 发言不设置不同产品类型。DM 在正文中自然表达 NPC 身份。

## 10.2 可见范围

消息可能是：

- `PUBLIC`：对发送时 Campaign 中的全部参与角色可见；
- `PRIVATE`：只对 DM 选择的一个或多个角色可见；
- `DM_ONLY`：Character Agent 发送，只对 DM 可见。

规则：

- DM 始终能看到全部消息；
- 消息保存发送时的接收者快照；
- 后来加入的角色不会获得加入前的公开消息；
- 未被选择的角色不知道私密消息存在；
- 发送 Agent 始终知道自己发送的 DM_ONLY 消息；
- 私密消息在主时间线中显示锁标识和接收者；
- 接收私密信息的角色可自主决定是否公开内容。

## 10.3 AI 的秘密行动

Agent 可以输出 `DM_ONLY`，用于秘密台词或秘密行动。

约束：

- 默认应使用 PUBLIC；
- 五至六名角色是长期冒险伙伴，系统不主动鼓励互相隐瞒；
- 只有 Prompt、既有秘密或当前情境提供明确保密动机时才使用 DM_ONLY；
- 不得为制造戏剧冲突而凭空产生秘密；
- DM_ONLY 不能用于内心独白或模型推理；
- 秘密行动需要外部结果时同样立即等待 DM。

## 10.4 消息不可修改原则

除 OOC 修订外：

- 消息发送后不能编辑；
- 不能撤回；
- 不能删除；
- 内容错误必须通过 OOC 纠正。

## 10.5 OOC 行为

每条 OOC 都会：

- 作为最新纠正信息发送给有权看到的角色；
- 使尚未发布的旧候选回复作废；
- 取消当前待裁决事项；
- 触发相关 Agent 重新判断；
- 不被当作世界内发生的对话；
- 不进入角色的场内记忆，但其纠正后的事实会影响认知。

OOC 支持公开或私密接收者。

## 10.6 最新消息 OOC 修订

MVP 只允许替代当前 Session 中最新一条有效的非 OOC 消息。

### 修订 AI 消息

1. DM 点击“纠正此消息”；
2. 输入 OOC 纠正内容；
3. 原 Character Agent 根据纠正重新生成；
4. 新消息通过权限检查；
5. 新消息替代原消息并显示“OOC 修订”标志；
6. 其他 Agent 只根据新版本继续判断。

如果角色在纠正后选择沉默：

- 原消息标记为“已由 OOC 作废”；
- 不生成替代气泡。

### 修订 DM 消息

- DM 必须在 OOC 表单中填写正确版本；
- 系统不能让 AI 代替 DM 生成世界事实；
- 正确版本替代原 DM 消息并显示“OOC 修订”标志。

### 底层保存

- 原始错误消息内部保留并标记 `superseded`；
- 普通 UI、Agent 上下文、摘要、记忆和导出只使用有效版本；
- OOC 修订是“消息不可修改”原则的唯一例外。

## 10.7 较早事实纠正

- 已结束 Session 保持只读，不能直接替换历史消息。
- 当前 Session 中较早的消息也不能回溯替换，以避免整段分支回滚。
- DM 可以在当前时间发送 OOC，并关联较早的错误消息，修正角色从此刻起的认知。
- 实时聊天不重新生成或移动较早消息；原消息只增加“后来已纠正”标志。
- Agent 的有效上下文不再把较早错误版本当作事实，而使用当前 OOC 的纠正内容。
- 旧 Session 原始记录保持不变，但当前摘要和长期记忆必须修正。
- 导出时仍按 OOC 的导出选择处理：省略错误消息，或在其逻辑位置使用正确内容替代。

## 10.8 气泡格式

- 一次 Agent 响应只发布一个气泡；
- 气泡可以较长并包含多段；
- 支持台词、动作或二者结合；
- 只使用纯文本并保留换行；
- 不渲染 Markdown 标题、表格或代码块；
- 单条消息硬上限 3000 字符；
- 不逐字流式显示；
- 不显示消息时间。

---

# 11. HP 与健康状态

## 11.1 DM 界面

DM 可以看到：

```text
当前 HP / 最大 HP
HP 条
```

点击后提供：

- 直接输入当前 HP；
- `-10 / -5 / -1`；
- `+1 / +5 / +10`；
- 设为 0；
- 恢复至最大；
- 保存。

约束：

- 当前 HP 限制在 `0～maxHp`；
- 最大 HP 只在角色详情页修改；
- HP 修改立即保存；
- HP 修改不触发 AI；
- HP 修改不产生聊天消息；
- 下一次 Agent 调用读取新的定性状态。

## 11.2 Agent 健康状态

Agent 不读取准确 HP，只读取自己和队友的定性健康状态：

```typescript
type HealthCondition =
  | "HEALTHY"
  | "LIGHTLY_HURT"
  | "HEAVILY_HURT"
  | "CRITICAL"
  | "UNCONSCIOUS";
```

阈值：

- `currentHp > 75% maxHp`：健康；
- `50% < currentHp <= 75%`：轻伤；
- `25% < currentHp <= 50%`：重伤；
- `0 < currentHp <= 25%`：濒危；
- `currentHp <= 0`：昏迷。

健康状态只是角色可感知的信息，不强迫角色采取最优或保守行为。

---

# 12. Character Agent Runtime

## 12.1 Agent 职责

```text
感知最新事件
→ 读取角色 Prompt、能力、健康、认知和记忆
→ 独立决定沉默或回应
→ 生成一个完整候选气泡
→ 声明可见范围、紧急性和是否需要 DM
```

## 12.2 上下文组成

每次 Agent 调用按以下顺序组装：

```text
1. 系统权限与输出规则
2. 当前角色 Roleplay Prompt
3. 当前 Character Development Profile
4. Excel 提取的实际能力、法术、身份背景和装备
5. 自己及队友的定性健康状态
6. 相关旧 Campaign 独立摘要与长期记忆
7. 当前 Campaign 的角色独立滚动摘要
8. 当前角色可见的最近原始消息
9. 最新触发事件
```

## 12.3 信息优先级

1. 系统权限规则最高；
2. 最新 OOC 纠正高于旧消息；
3. DM 最新场内事实高于角色猜测和旧记忆；
4. Roleplay Prompt 定义核心身份和初始人格；
5. Character Development Profile 和记忆反映长期变化；
6. Excel 只定义实际能力，不定义人格；
7. 角色可以保留误解，但 DM 明确纠正后必须更新；
8. 私密过滤必须在摘要和上下文构建前完成。

## 12.4 强制认知隔离

> Character Agent 只能获得该角色亲自经历、被公开告知、被私密告知或由自身合理记忆保留的信息。系统不得因为信息存在于数据库、DM 摘要、其他角色记忆或加入前的聊天中，就将其提供给该角色。

进入新 Campaign 时，Agent 可以读取：

- 自己参与过的旧 Campaign 独立摘要；
- 自己的长期记忆；
- 当前成长档案。

但不能读取：

- 旧 Campaign 中自己无权看到的消息；
- DM 总览摘要；
- 其他角色记忆；
- 当前 Campaign 加入前的消息或摘要。

## 12.5 输出协议

```typescript
interface AgentReaction {
  decision: "SILENCE" | "RESPOND";

  message?: string;

  responseType?:
    | "SPEECH"
    | "ACTION"
    | "SPEECH_AND_ACTION";

  visibility?: "PUBLIC" | "DM_ONLY";

  urgency?: "NORMAL" | "HIGH" | "IMMEDIATE";

  addressedCharacterIds?: string[];

  requiresDmResolution?: boolean;
  resolutionRequest?: string;
}
```

不再使用：

- `CONTINUE`；
- `AWAIT_CHARACTER`；
- `AWAIT_DM`。

角色提问只需正常发出消息，被点名角色会获得下一次排序优先级。

## 12.6 可观察内容原则

公开或 DM_ONLY 气泡只能包含：

- 台词；
- 动作；
- 姿态；
- 神态；
- 声音；
- 其他可以在世界中观察到的表现。

不得包含：

- 角色内心独白；
- 模型推理；
- “作为 AI”“根据设定”等出戏表达；
- 其他角色无法观察却被当作公开事实的心理内容。

## 12.7 叙事权限

### 无需等待 DM

- 表达观点和情绪；
- 提出计划；
- 向队友提问；
- 拔出自己的武器；
- 拿出已知随身物品；
- 在没有明显障碍时进行普通移动；
- 执行不依赖外部结果的自身动作。

### 需要 DM

- 攻击是否命中；
- 调查或观察发现什么；
- NPC 如何回答；
- 环境如何变化；
- 门后有什么；
- 法术或能力产生什么外部效果；
- 检定、伤害、治疗和规则结果；
- 秘密行动是否被发现。

消息必须停在行动已经发起的位置，不能继续假定结果。

## 12.8 核心全局 Prompt 约束

```text
你正在完整扮演指定角色，而不是以助手或玩家身份帮助用户完成任务。

你的核心人格来自 Roleplay Prompt；长期经历、关系和态度变化来自 Character Development Profile 与角色记忆。Excel 只描述你实际拥有的身份、能力、法术和物品。

你可以自主决定是否说话、说什么以及采取什么行动。你不必强行推进剧情，也不必永远选择战术最优行为。但你与其他角色是长期冒险伙伴，除非你的设定和经历提供明确理由，否则默认公开协作，不主动制造秘密或冲突。

你只能使用当前输入中对该角色可知的信息。不得读取其他角色的秘密，不得把猜测写成世界事实。

你可以决定自己的行为，但不能替其他角色、NPC、敌人或环境决定反应。需要世界或规则反馈时，停在行动已经发起的位置，并等待 DM。

你没有修改 HP、法术位、物品、状态或任何游戏数据的能力。不要输出数据修改指令。

你可以理解能力的完整规则，但不得说出属性值、技能值、AC、DC、攻击加值、伤害骰、法术位数量或其他机械数值。不要主动申请某项技能检定；直接描述你想做什么。

消息只包含可观察的台词、动作和外在表现。不要展示角色内心独白、模型推理或出戏表达。
```

---

# 13. Multi-Agent Orchestrator

## 13.1 定位

Orchestrator 是运行秩序协调器，不拥有角色意志。

负责：

- 接收新消息事件；
- 根据可见范围确定 eligible Characters；
- 并行询问各角色；
- 接收沉默或完整候选；
- 排定下一位发言者；
- 验证最终候选；
- 发布一条完整消息；
- 将新消息重新广播；
- 处理等待 DM、停止、错误和上限；
- 保证旧候选失效；
- 防止重复发布。

不负责：

- 决定角色应该做什么；
- 选择最优战术；
- 强制合作；
- 改写角色选择；
- 控制 NPC 或世界；
- 修改游戏数据。

## 13.2 事件驱动模型

系统不使用“回合”或“轮次”作为叙事概念。

```text
已发布消息
→ 成为最新事件
→ 所有有权感知的角色独立判断
→ 选择一个最终候选
→ 验证并发布一个完整气泡
→ 新气泡成为下一事件
```

全员沉默后进入 IDLE，等待 DM 新输入。系统不使用后台定时器让角色突然发言。

## 13.3 运行状态

```typescript
type SessionRuntimeStatus =
  | "IDLE"
  | "AGENTS_EVALUATING"
  | "VALIDATING_MESSAGE"
  | "WAITING_FOR_DM"
  | "ERROR"
  | "ENDED";
```

- `IDLE`：等待 DM 或新事件；
- `AGENTS_EVALUATING`：角色独立判断；
- `VALIDATING_MESSAGE`：检查最终候选；
- `WAITING_FOR_DM`：等待外部裁决；
- `ERROR`：所有相关调用失败；
- `ENDED`：Session 已结束。

不设置 `PAUSED`，也不恢复旧运行现场。

## 13.4 Agent 调用原则

- 每条新消息发送给所有有权看到的 Campaign 角色；
- 每个角色使用独立 Agent 调用；
- 角色可以返回 SILENCE；
- 刚发言角色仍可再次回应，但获得软降权；
- 直接被点名或存在紧急反应时不受降权影响；
- 每个 Agent 一次性返回沉默或完整候选气泡；
- 未被选中的候选不会发布。

## 13.5 发言排序

优先级：

1. 被最新消息直接点名或提问；
2. `IMMEDIATE` 或紧急反应；
3. 更高 urgency；
4. 更久没有发言；
5. 完全相同时随机选择。

发布一条消息后：

- 所有未发布旧候选作废；
- 新消息重新广播；
- 所有相关角色重新判断；
- 不按固定角色顺序轮流发言。

## 13.6 等待 DM

当最终消息 `requiresDmResolution = true`：

- 消息正常发布；
- 显示：`等待 DM：{resolutionRequest}`；
- 所有 AI 立即停止；
- 不继续收集其他待裁决行为；
- 系统进入 WAITING_FOR_DM。

DM 在 WAITING_FOR_DM 时发送任意 IN_GAME 消息：

- 视为完成当前裁决；
- 清除等待状态；
- 新 DM 消息成为最新事件；
- 相关角色重新判断。

公开待裁决默认使用公开 DM 回复；DM_ONLY 待裁决默认继承为对该角色私密回复。DM 发送前可以调整接收者。

发送 OOC 会取消待裁决事项并要求相关 Agent 重新处理。

## 13.7 发布前检查

只检查最终要发布的候选，避免检查全部废弃候选。

检查内容：

- 是否替 NPC、环境或规则决定结果；
- 是否把未知信息写成事实；
- 是否包含机械数值或检定术语；
- 是否泄露不可见信息；
- 是否正确标记需要 DM；
- 是否遵守单气泡和可观察内容规则。

处理：

- 仅漏标等待：系统补为 `requiresDmResolution = true`；
- 正文已经越权：退回原 Agent 重新生成；
- 检查器只能通过、要求等待或退回，不能替角色改写内容；
- 连续失败达到有限重试上限后，按 Agent 失败处理。

## 13.8 自动对话上限

- 两条 DM 消息之间最多连续发布 12 条 AI 消息；
- 只计算实际发布的角色气泡；
- 沉默判断和废弃候选不计数；
- 达到上限后回到 IDLE；
- 页面提示已达到自动对话上限；
- 不提供“恢复旧运行”按钮；
- DM 发送新场内消息后重新启动。

## 13.9 DM 停止与新消息优先

“停止 AI 自动对话”：

- 取消尚未完成或尚未发布的内容；
- 已发布消息保留；
- 旧候选作废；
- 回到 IDLE；
- 不提供继续旧 Run。

DM 输入框始终可用。AI 运行期间 DM 发送新消息：

- 等同于接管对话；
- 尚未发布的输出作废；
- 已发布消息保留；
- 新 DM 消息立即成为最高优先级事件。

## 13.10 全员沉默

所有角色均选择 SILENCE 时：

- 回到 IDLE；
- 输入框上方显示“当前没有角色选择回应，等待 DM 输入”；
- 不生成系统聊天消息；
- 不写入长期聊天历史。

## 13.11 错误恢复

- 单个 Agent 失败自动重试一次；
- 再次失败时，本事件将该角色视为未响应；
- 其他角色继续正常处理；
- DM 看到临时技术状态提示；
- 错误不向其他角色暴露；
- 所有相关角色都失败时进入 ERROR；
- 提供“重试最新事件”；
- 重试不重复保存触发消息；
- 失败内容不进入摘要或记忆。

## 13.12 多标签页与刷新

- 可以用多个标签页查看同一 Campaign；
- 同一 Campaign 同时只有一个有效自动对话流程；
- 最新 DM 消息使旧候选失效；
- 刷新或关闭浏览器不取消后端正在运行的 Agent Run；
- 重新打开后恢复最新消息和状态；
- 整个后端程序关闭时，未完成 Run 在重启后标记失败，可重试最新事件。

---

# 14. Context、Memory 与角色成长

## 14.1 三层当前记忆

每个角色在当前 Campaign 中独立维护：

| 层级 | 内容 |
|---|---|
| Recent Context | 该角色可见的最近原始消息 |
| Rolling Session Summary | 当前 Session 较早内容的角色独立摘要 |
| Long-Term Character Memory | 跨 Session、跨 Campaign 保留的重要经历 |

不使用固定消息条数作为上下文上限，按 Token 预算裁剪。

必须优先保留：

- 最新 OOC；
- 最新 DM 场内事实；
- 最新触发事件；
- 最近原始消息；
- Roleplay Prompt；
- 当前能力资料。

较旧内容由独立摘要和相关记忆替代。

## 14.2 DM 总览摘要

每个 Session 结束时生成一份 DM 总览摘要：

- 可以包含公开和全部私密内容；
- 只供 DM 查看；
- 不发送给任何 Agent；
- MVP 中只读，DM 不编辑；
- 可用于 Campaign 导出。

## 14.3 角色独立摘要

每个角色单独生成 Session 摘要：

- 只使用该角色实际可见消息；
- 不读取其他角色秘密；
- 不读取 DM 总览摘要；
- 不为中途加入角色补写加入前内容；
- OOC 只用于修正事实，不作为世界事件写入；
- 摘要不能创造未发生事实。

## 14.4 长期记忆

Session 结束时，从角色可见内容中提取值得跨 Session 保存的记忆。

每条记忆包含：

```typescript
interface CharacterMemory {
  id: string;
  characterId: string;
  content: string;
  sourceType: "CAMPAIGN" | "DM_MANUAL";
  sourceCampaignId?: string;
  sourceSessionId?: string;
  pinned: boolean;
}
```

不包含：

- 数值重要度；
- 情绪分数；
- 好感度；
- 数值关系值。

DM 可以：

- 查看；
- 添加；
- 编辑；
- 删除；
- 标记固定。

由 DM 手动添加的记忆使用 `DM_MANUAL` 来源，不会因删除或重置某个 Campaign 而被清除。由跑团经历生成的记忆必须记录来源 Campaign 与 Session。

自动生成的长期记忆在生成成功后立即用于后续 Session，不要求 DM 先审核通过。DM 可以稍后修正；开始下一次 Session 不被记忆审核阻塞。

固定记忆不会被自动合并或淡化。

## 14.5 Character Development Profile

每个全局角色拥有一份可编辑的当前成长档案：

```text
当前性格表现与新形成的习惯
长期目标和未解决问题
重要关系与当前态度
创伤、承诺与价值观变化
```

规则：

- Roleplay Prompt 保持核心身份和初始人格；
- 系统不自动改写 Roleplay Prompt；
- 长期经历可以通过成长档案影响行为；
- 每次 Campaign 完成时自动更新成长档案；
- DM 可以随时手动编辑；
- 重新开启 Campaign 后，后续完成时继续增量更新；
- 删除或重置 Campaign 后，移除来源记忆并重新生成成长档案。

## 14.6 长期记忆整理与检索

- 不设置长期记忆条数产品上限；
- 数据库保留全部记忆；
- 系统可合并重复或过时记忆；
- 不删除原始 Campaign 消息；
- 每次 Agent 调用只选择固定记忆和与当前事件相关的记忆；
- 旧 Campaign 原始聊天全文不直接进入新 Campaign 上下文；
- 角色通过旧 Campaign 独立摘要、成长档案和相关记忆理解过去。

## 14.7 关系表达

- 不建立 CharacterRelationship 数值系统；
- 关系作为角色自己的定性记忆和成长档案内容；
- 甲对乙的看法可以与乙对甲不同；
- 关系不要求对称；
- 允许同时存在信任、戒备、愧疚和责任等复杂态度。

---

# 15. 机械资源边界

MVP 只严格维护 HP。

对于法术位、能力次数和消耗品：

- Agent 读取完整能力说明；
- Agent 根据可见聊天历史尽量记住已使用资源；
- Agent 尽量遵守短休、长休和使用限制；
- 系统不建立严格运行状态；
- AI 不输出资源扣除指令；
- DM 的场内休息描述作为恢复依据；
- 错误使用耗尽资源时，DM 使用 OOC 纠正并让 Agent 重新处理；
- MVP 不承诺资源追踪机械准确。

若未来要求严格准确，需要另行设计 DM 手动资源面板，不属于当前范围。

---

# 16. 导出

## 16.1 Campaign 导出

导出 ZIP：

```text
campaign.json
sessions/{session-title}.md
characters/{character-name}.md
summaries/dm-summary.md
```

包含：

- 该 Campaign 的最终有效公开消息；
- DM 私密消息；
- Agent DM_ONLY 消息；
- Session 摘要；
- 参与角色当前 Roleplay Prompt；
- 来源属于该 Campaign 的角色记忆；
- 消息发送者和可见范围标记。

不包含：

- 原始错误消息；
- 被修订 OOC 本身；
- 角色在其他 Campaign 中获得的秘密或记忆；
- 头像；
- 原始 Excel；
- UI 时间戳。

OOC 导出规则：

- “使原消息无效”：省略错误消息和 OOC；
- “用纠正内容替换”：在原逻辑位置输出正确版本，不显示 OOC 标签；
- Markdown 和 `campaign.json` 使用同一有效消息视图。

MVP 不提供 Campaign 导入恢复。

## 16.2 角色导出

全局角色库提供“导出角色”，包含：

- 当前 Roleplay Prompt；
- 当前 Character Development Profile；
- 全部长期记忆，按来源 Campaign 或 DM 手动来源分组；
- 当前实际能力、法术、装备和背包解析结果。

不包含：

- 原始 Excel；
- 头像；
- 其他角色的秘密或记忆。

## 16.3 重置与删除前提醒

重置或永久删除确认窗口提供“先导出记录”，但不强制执行。

---

# 17. 自动保存与本地数据

## 17.1 自动保存

立即保存：

- 已发布消息；
- OOC 修订关系；
- 当前 HP 修改；
- 最大 HP 修改；
- Prompt 修改；
- Excel 解析快照；
- 角色成长档案修改；
- 长期记忆修改；
- Campaign 与 Session 状态。

不提供“保存整个战役”按钮。

## 17.2 数据归属

- Character 全局配置归属于 Character Library；
- Campaign Membership 关联全局 Character；
- 每条 Session、消息、摘要、HP 状态和 Agent Run 归属于唯一 Campaign；
- 每条长期记忆归属于 Character；跑团生成的记忆记录来源 Campaign 与 Session，手动记忆记录 DM_MANUAL 来源；
- 不允许跨 Campaign 混用当前消息或运行状态。

## 17.3 本地单用户

- MVP 只供一个 DM 使用；
- 不做登录；
- 不做用户注册；
- 不做权限多租户；
- 只在本机运行；
- 代码可以上传 GitHub；
- API Key、运行数据库和上传文件不得提交 Git；
- 运行数据使用项目外可配置目录保存。

建议的数据目录概念：

```text
AI_TRPG_DATA/
  app-data
  uploads/
    avatars/
    character-sheets/
  exports/
```

具体数据库和目录实现留待技术设计。

---

# 18. 非功能需求

## 18.1 稳定性

- 页面刷新后恢复当前 Session 和消息；
- 关闭浏览器不自动结束 Session；
- 单个 Agent 失败不破坏整个流程；
- Excel 解析失败不覆盖旧角色卡；
- 不允许重复发布同一角色消息；
- OOC 修订后无效版本不得继续进入 Agent 上下文；
- Session、Campaign 和记忆生成失败均提供可恢复状态；
- 所有关键写入自动保存。

## 18.2 认知安全

- 私密消息不能进入无权角色的上下文；
- DM 总览摘要不能进入任何 Agent；
- 新角色不能读取加入当前 Campaign 前的内容；
- 删除或重置 Campaign 后，相关来源记忆必须移除；
- Excel 数据库页和未选能力不能进入角色能力快照；
- 候选回复不得因系统日志或其他 Agent 数据产生信息泄露。

## 18.3 文件安全

- 验证扩展名、内容类型和文件大小；
- 文件名不直接作为服务器路径；
- 解析失败文件不成为有效角色卡；
- 导出文件只使用有效消息视图；
- Git 仓库不包含运行数据和密钥。

## 18.4 可观测性

正常跑团页面只显示：

- 哪些角色正在判断；
- 正在验证消息；
- 等待 DM；
- 达到自动对话上限；
- 单角色响应失败；
- 全部调用失败。

独立开发调试页记录：

- Agent Run ID；
- 触发事件；
- 被调用角色；
- 角色是否沉默；
- 发言排序结果；
- 模型名称；
- 输入与输出 Token；
- 调用耗时；
- 重试次数；
- 停止原因；
- 结构化输出错误；
- 权限检查结果。

不展示或保存为用户可见内容：

- 模型隐藏推理；
- Chain of Thought；
- 被废弃候选正文，除非开发日志策略在技术阶段另行确认。

不在主界面计算货币成本。不同供应商价格和免费额度由后续技术配置决定。

## 18.5 桌面端范围

- 只保证电脑浏览器体验；
- 不做移动端适配；
- 不做原生桌面应用；
- 不做聊天附件；
- 页面交互优先满足鼠标和键盘。

---

# 19. 核心用户流程

## 19.1 创建全局角色

```text
进入角色库
→ 创建角色
→ 输入显示名称
→ 上传可选头像
→ 填写或导入 Roleplay Prompt
→ 上传固定模板 Excel
→ 输入最大 HP
→ 查看解析预览
→ 确认
→ 创建全局角色
```

## 19.2 筹备 Campaign

```text
创建 Campaign
→ 输入名称与可选描述
→ 从角色库选择 1～6 名角色
→ 检查角色配置完整性
→ 确认当前没有其他 ACTIVE Campaign
→ 启动 Campaign
```

## 19.3 开始 Session

```text
进入 ACTIVE Campaign
→ 创建或继续 Session
→ 恢复各角色当前 HP
→ 进入跑团主页面
→ AI 保持安静
→ DM 发送第一条 IN_GAME 消息
```

## 19.4 DM 消息触发角色

```text
DM 发送消息
→ 保存消息和接收者快照
→ 取消旧候选
→ 可见角色独立判断
→ Orchestrator 排序
→ 最终候选通过发布前检查
→ 一次性发布完整气泡
→ 新气泡重新广播
```

## 19.5 等待 DM

```text
角色声明需要外部结果的行为
→ 发布完整角色气泡
→ 显示具体待裁决事项
→ 所有 AI 停止
→ DM 发送 IN_GAME 裁决
→ 清除等待
→ 相关角色根据最新事实重新判断
```

## 19.6 OOC 修订最新 AI 消息

```text
DM 点击“纠正此消息”
→ 输入 OOC
→ 旧候选和待裁决失效
→ 原角色重新生成或选择沉默
→ 验证新版本
→ 替代原气泡并标记“OOC 修订”
→ 修正摘要与记忆
→ 其他角色基于有效版本继续
```

## 19.7 结束 Session

```text
DM 点击结束 Session
→ 取消未发布输出
→ 保存最终 HP
→ 生成 DM 总览摘要
→ 生成各角色独立摘要
→ 提取长期记忆
→ Session 只读
```

## 19.8 完成 Campaign

```text
DM 结束当前 Session
→ 点击结束战役
→ Campaign 变为 COMPLETED
→ 更新参与角色成长档案
→ Campaign 默认只读
```

## 19.9 重置 Campaign

```text
点击重置跑团进度
→ 可选先导出
→ 二次确认
→ 删除 Session、消息、摘要和来源记忆
→ 重建角色成长档案
→ 保留角色配置与阵容
→ Campaign 回到 PREPARATION
```

---

# 20. 推荐开发阶段

本节只描述产品交付顺序，不锁定技术栈。

## Phase 1：全局角色库与 Campaign 骨架

- 全局角色 CRUD 中的创建与编辑；
- 头像；
- Prompt 导入、编辑和导出；
- Excel 上传、解析、预览和替换；
- 最大 HP；
- Campaign 生命周期；
- PREPARATION 阵容选择；
- 单一 ACTIVE Campaign 约束。

## Phase 2：单角色跑团闭环

- Session；
- 两栏跑团页面；
- DM IN_GAME；
- 单 Character Agent；
- 完整气泡原子发布；
- 叙事权限检查；
- WAITING_FOR_DM；
- 停止与错误恢复；
- HP 定性状态。

## Phase 3：Multi-Agent 自主讨论

- 1～6 个独立 Agent；
- 并行角色判断；
- Speaker Coordinator；
- 消息重新广播；
- 12 条连续消息上限；
- 点名、紧急性与发言公平排序；
- DM 最新消息优先；
- 全员沉默处理。

## Phase 4：私密信息与 OOC 修订

- DM 私密消息；
- Agent DM_ONLY；
- 私密裁决继承；
- OOC 纠正与重新处理；
- 最新消息替代版本；
- 有效消息视图；
- 严格认知隔离。

## Phase 5：记忆、角色成长与导出

- 角色独立滚动摘要；
- DM 总览摘要；
- Session 长期记忆提取；
- 记忆管理页面；
- Character Development Profile；
- 跨 Campaign 记忆；
- Campaign 完成、重置和删除后的成长重建；
- Campaign 与角色导出。

## Phase 6：稳定性与调试

- Token 与耗时记录；
- 独立调试页；
- 多标签页冲突保护；
- 后端重启后的失败恢复；
- 文件和数据安全；
- 最终验收测试。

---

# 21. MVP 验收标准

## 21.1 全局角色

- 可以创建和编辑全局角色；
- 创建时除头像外均为必填；
- 角色不绑定单一 Campaign；
- 角色可以参与多个先后发生的 Campaign；
- MVP 不提供角色删除；
- Prompt、头像、Excel 和最大 HP 可以更新；
- 更新只在 AI 非运行状态进行；
- 历史消息保留发送时快照。

## 21.2 Excel 与机械信息

- 只接受固定模板 `.xlsx`；
- 解析前验证文件；
- 解析结果需要 DM 预览确认；
- 只提取实际拥有的身份、能力、法术、装备和背包；
- 不把完整法术库或职业库提供给 Agent；
- 不把 Excel 人格和故事提供给 Agent；
- Agent 能理解完整能力说明；
- Agent 输出不出现机械数值和申请检定表达；
- 替换失败不覆盖当前有效角色卡；
- 成功替换后只保留当前版本。

## 21.3 Campaign

- 同时只允许一个 ACTIVE Campaign；
- 可以筹备多个 PREPARATION Campaign；
- 每个 Campaign 当前最多 6 名角色；
- COMPLETED Campaign 默认只读；
- 可以在无其他 ACTIVE Campaign 时重新开启；
- 归档与生命周期分离；
- ACTIVE Campaign 不能归档；
- 重置保留全局角色配置；
- 永久删除不删除全局角色；
- 删除和重置会移除来源记忆并重建成长档案。

## 21.4 Session

- DM 手动创建和结束；
- 页面关闭不结束 Session；
- 每个 Campaign 同时最多一个未结束 Session；
- 新 Session 继承上一 Session 最终 HP；
- 历史 Session 只读；
- 开场必须由 DM 先发送消息；
- 结束时生成 DM 摘要、角色摘要和长期记忆；
- 单角色记忆失败不阻止 Session 结束。

## 21.5 消息与 OOC

- DM 场景与 NPC 台词统一为 IN_GAME；
- DM 可以公开或私密发送；
- 私密消息不会泄露给无权角色；
- 普通消息不可编辑、删除或撤回；
- OOC 会纠正并让 Agent 重新处理；
- 最新 AI 消息可以由同一 Agent 重新生成替代；
- 最新 DM 消息必须由 DM 提供正确版本；
- 有效版本进入上下文和导出；
- 原始错误版本不影响摘要或记忆；
- 导出只包含正确内容。

## 21.6 Multi-Agent

- 每个角色使用独立 Prompt、能力、认知和记忆；
- 每条事件询问所有有权感知的角色；
- 角色可以沉默；
- 不要求固定轮流；
- 同一角色可以连续回应但受到软降权；
- 点名和紧急反应优先；
- 一次只发布一个完整气泡；
- 未发布候选在新事件后失效；
- 两条 DM 消息之间最多 12 条 AI 消息；
- 全员沉默后不会后台定时自发发言。

## 21.7 叙事权限

- AI 可以决定自身台词和行动意图；
- AI 不替 NPC、环境或其他角色决定反应；
- 攻击不会自行宣布命中；
- 调查不会自行创造发现；
- 法术不会自行宣布外部效果；
- 第一个需裁决行为出现后所有 AI 停止；
- 页面显示明确待裁决事项；
- DM IN_GAME 回复解除等待；
- OOC 取消无效待裁决并重新处理。

## 21.8 可观察内容与秘密

- AI 气泡只包含可观察内容；
- 不展示内心独白；
- 不展示模型推理；
- DM_ONLY 只用于合理的秘密台词或行动；
- 系统默认鼓励长期队伍公开协作；
- 秘密行为需要结果时同样等待 DM；
- 私密裁决默认继承原可见范围。

## 21.9 HP

- 最大 HP 由 DM 手动输入；
- 修改最大 HP 时 ACTIVE Campaign 当前 HP 同步为最大；
- DM 可以快速修改当前 HP；
- 当前 HP 不超出 `0～maxHp`；
- HP 修改不触发 AI；
- Agent 看不到准确 HP；
- Agent 能看到全队定性健康状态；
- 健康阈值符合本文定义。

## 21.10 认知与长期成长

- 每个角色摘要只来自其可见消息；
- DM 总览摘要不发送给 Agent；
- 中途加入角色不知道加入前内容；
- 角色进入新 Campaign 时保留自己真实获得的旧经历；
- 不读取旧 Campaign 中无权看到的秘密；
- Session 结束产生长期记忆；
- Campaign 完成更新成长档案；
- DM 可以编辑成长档案和记忆；
- 不使用数值关系系统；
- 删除或重置 Campaign 后相关经历不再影响角色。

## 21.11 稳定性与导出

- 刷新页面恢复消息和状态；
- DM 可以随时停止自动对话；
- DM 新消息可以取代未完成输出；
- 单 Agent 失败不阻塞其他角色；
- 全部失败可重试最新事件；
- 已发布消息不重复保存；
- Campaign 导出不包含错误版本；
- 角色导出包含 Prompt、成长档案和全部来源记忆；
- 重置和删除前提供导出入口；
- Token、模型和耗时可在调试页查看。

---

# 22. 技术阶段待确认事项

产品需求确认后，技术设计至少需要单独讨论：

1. 前端框架与组件体系；
2. 后端框架；
3. 本地数据库与迁移方案；
4. 前后端实时事件协议；
5. Agent Run 的并发、取消与幂等实现；
6. DeepSeek、Gemini 或其他 LLM 供应商选择；
7. Character Model 与 Utility Model 是否分离；
8. Structured Output 能力与兼容策略；
9. Token 预算与上下文裁剪算法；
10. 长期记忆相关性检索实现；
11. Excel 模板的单元格映射与版本验证；
12. 本地文件目录、启动脚本和密钥配置；
13. 调试日志对废弃候选正文的保存策略；
14. 自动测试、端到端测试与验收数据集；
15. 未来角色上限扩展方案。

在上述技术项确认前，本文可以用于 UI 原型、任务拆分、数据模型草拟和验收场景设计，但不应被视为最终技术架构说明。
