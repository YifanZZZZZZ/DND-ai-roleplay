# 角色校准方案评估与合并建议

对 Codex《角色扮演 Agent · 角色校准方案》的可行性与成本评估，以及与本项目现状结合后的落地路径。

---

## 一、总体判断

方向是对的，而且和我上一轮给你的六个方案高度重合——两边独立得出同样结论，说明这条路基本没跑偏：

| Codex 的提法 | 我上一轮的方案 |
|---|---|
| Behavior Rules：情境 → 判断 → 行动 | 方案一：人设改写成 if-then 规则 |
| Expression 里的「语言禁区」 | 方案二：每个角色一份「绝不」清单 |
| 用户评价 / OOC 修正回流成规则 | 方案六：从真实跑团记录蒸馏 |
| Appraisal → Decision → Expression | 方案五：生成-自评-重写的变体 |

**它比我的方案强在三点**，这三点我上一轮没想到：

1. **主动诊断，而不是被动等待。** 我的方案六是跑团时打标，要靠场景自己撞上来；它是主动去找 Character Model 里最不确定的地方，专门造一个场景去测。收敛快得多。

2. **阶段 B 的 decision boundary。** 「帮助陌生人 → 帮助骗子 → 帮助敌人 → 帮助会危及队友的敌人」——用连续变条件去找角色在哪里崩。这是整份文档最好的一个想法。人格不是由形容词定义的，是由**它在哪里失效**定义的。而且这正好打你现在最痛的点：维瑞娅、赛蕾妮、卡莱拉都「会帮人」，但三个人的断点位置完全不同。

3. **阶段 C 的价值冲突排序。** 「都很重要」正是趋同的根源；只有优先级才能区分。让好奇心和队友安全打架，三个人的选择立刻分开。

---

## 二、三个会拖垮成本的地方

### 2.1 三段式推理不要拆成三次调用

文档建议把 RP 拆成 Appraisal → Decision → Expression。**理由是对的**（避免语言风格反过来主导人物行为），**但实现方式会让成本翻三倍**。

现在一轮 5 个角色 = 5 次调用。拆成三段 = 15 次，延迟也接近三倍。

**改法：拆成同一次调用里的三个有序字段。** 结构化输出是自上而下生成的，排在 `content` 之前的字段就是在台词存在之前被决定的。我们已经用 `inner_beat` 和 `requires_dm_resolution` 验证过这个机制有效。

```python
class CharacterDecision(BaseModel):
    decision: Literal["SILENCE", "RESPOND"]
    appraisal: str      # 她如何理解当前局面
    intent: str         # 按她的价值观，她想做什么
    requires_dm_resolution: bool
    resolution_request: str | None
    content: str        # 她会怎么把这个决定表现出来
    ...
```

同样是三段分离，成本从 3× 降到约 1.3×（只多几十个输出 token），延迟几乎不变。这是全文我最想让你改的一条。

### 2.2 不要重建 Character Model，做增量迁移

文档提议一个全新的五模块 Character Model。但你已经有五份写得相当好的 `roleplay_prompt`，六个段落几乎正好能映射过去：

| Codex 模块 | 你现有的段落 | 缺什么 |
|---|---|---|
| Identity | 身份 + 简短背景 | 无 |
| Values | 价值与动机 | **优先级排序** |
| Beliefs | 私人信息 · 可能存在的偏见或误解 | 无 |
| Behavior Rules | 行为与伙伴 | **if-then 化** |
| Expression | 说话与外在表现 + 说话范例 | **禁区清单** |

真正缺的只有三样：价值优先级、if-then 行为规则、语言禁区。

**改法：保留 `roleplay_prompt` 原文不动**，新增两个结构化字段（`behavior_rules`、`expression_bans`）和一个 `value_priority`。这样校准产出的东西有地方存，但你已经写好的人设一个字都不用重写，也不会在迁移过程中丢掉那些很有味道的描述（「像一只戒备的小刺猬」这种句子，结构化之后大概率会被磨平）。

### 2.3 冷启动的 DM 时间成本被低估了

阶段 A + B + C 全跑，每个角色 20～40 轮问答，五个角色就是 100～200 轮。这不是模型成本问题（DeepSeek 便宜），是**你的时间**问题。

**改法一：批量校准。** 一次生成一个场景，**五个角色同时作答，并排展示**，你一次判断五个。

- 调用数：`1 场景生成 + 5 个角色作答 + 1 规则抽取 = 7 次/场景`，而不是逐角色的 `3 × 5 = 15 次`
- 你的判断次数：从 5 次降到 1 次（并排看比分别看快得多）
- **额外好处：趋同问题会当场暴露。** 如果三个人的回答看起来差不多，那这个场景就是最该校准的地方——这直接就是你现在最大的痛点

**改法二：最小可用校准。** 不做穷尽式的阶段 A。先挑 6～8 个**高区分度场景**——专门找三个克制型角色会分叉的地方（被道谢、队友犯错、陌生人示好、有人踩雷区、要不要丢下伤员、好奇心 vs 安全）。跑完这 8 个，区分度就该出来了。剩下的边界和冲突留给正式跑团时的 OOC 慢慢补。

---

## 三、两条建议补上的

### 3.1 诊断场景天然就是回归评测集

文档里提到了规则冲突检查，但那是**静态**的（新规则和旧规则文本上矛不矛盾）。真正危险的是**行为回归**：为了修卡斯珀加的一条规则，把赛蕾妮弄坏了。

Calibration Agent 生成的每个场景，连同你确认过的「正确反应」，直接存起来当回归集。每次 Character Model 更新后重跑一遍，对比新旧输出。

这几乎零额外设计成本——场景本来就要生成，答案本来就要你确认，只是别扔掉。我上一轮说的「评测集」，在这个方案里是免费附赠的。

### 3.2 场景要从真实模组和真实队友里取材

抽象场景（「如果有人向你求助」）会得到抽象回答，抽出来的规则也是抽象的，回到跑团里照样不管用。

Calibration Agent 应该读**当前模组和当前队伍**来造场景：用维尔梅隆的废墟、用卢卡斯的请求、用队伍里真实的另外四个人。这样校准出的规则直接可用，而且顺带测了角色关系。

### 3.3 confidence 必须影响某个具体行为，否则就是装饰

文档给每条规则加了 confidence，但没说它怎么用。一个不影响任何行为的数值就是摆设。

**我的建议：低置信度规则不注入 system prompt，只用来驱动 Calibration 的下一轮选题。** 把没验证过的推测喂给扮演 Agent，等于用猜测污染人设——还不如让它按已确认的部分演，把不确定的地方留白。

---

## 四、成本估算

以 5 个角色、8 个高区分度场景的最小可用校准计：

| 项目 | 调用数 | 你的时间 |
|---|---|---|
| 批量校准（8 场景 × 7 次） | ~56 | 8 次并排判断，约 30～45 分钟 |
| 逐角色校准（对照） | ~120 | 40 次判断，约 2～3 小时 |
| 正式 RP 每轮（内联三段） | 5 次，约 1.3× token | 不变 |
| 正式 RP 每轮（三次调用，对照） | 15 次，延迟 ~3× | 不变 |

模型成本在 DeepSeek 上可以忽略。真正的成本是你的判断时间和跑团时的延迟，两者都靠上面两条改法压下来。

---

## 五、建议的落地顺序

1. **先加 `expression_bans`（语言禁区）和 `behavior_rules`（if-then）两个字段**，手工给一个角色填上，看效果。不做任何 Agent，纯 prompt 改动，半天能验证方向对不对。
2. **把 `inner_beat` 拆成 `appraisal` + `intent`**，同一次调用。这是零成本的结构改动。
3. **做批量 Calibration**：一次一个场景、五人并排、你打标、抽规则回填。跑 8 个高区分度场景。
4. **把这 8 个场景固化成回归集**，之后每次改人设都重跑。
5. 稳定之后再考虑 Episodic Memory / Relationship State / Character Development 那些扩展。

第 1、2 步不依赖第 3 步，可以今天就做。
