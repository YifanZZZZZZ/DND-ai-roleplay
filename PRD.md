# AI TRPG 网站 MVP 产品需求

> 本文是当前产品需求的入口与增量规则来源。既有功能的完整规则与验收标准见 [AI_TRPG_Website_MVP_PRD_v0.3.md](./AI_TRPG_Website_MVP_PRD_v0.3.md)；若旧版完整文档与本文关于 AI DM、技能检定或 Campaign 连续时间线的规则冲突，以本文为准，待下一版完整 PRD 统一合并。

## 1. 产品定位

AI TRPG 是一个由真人 DM 主持、AI DM 辅助叙事、多个 AI Character Agent 参与的本地网页跑团工具。

产品目标不是自动生成一个没有人类主持的游戏，也不是让多个通用助手协作完成任务，而是长期塑造一组持续存在的角色：他们拥有独立的人格、能力、经历、认知边界和记忆，并随着多次 Campaign 逐渐变得更加丰富。

### 1.1 核心角色

#### 真人 DM

DM 是唯一世界与规则裁决者，负责：

- 描述场景、NPC 和世界变化；
- 决定攻击、检定、法术、伤害和治疗结果；
- 发布公开或私密信息；
- 使用 OOC 纠正错误；
- 维护 HP；
- 管理 Character 和 Campaign；
- 停止 AI 自动对话；
- 编辑角色摘要、长期记忆和成长档案。

#### Character Agent

Character Agent 只扮演自己的角色，可以：

- 根据 Roleplay Prompt、说话范例、与其他角色的动态关系、完整既往故事、能力和当前认知思考；
- 自主决定说话、行动或沉默；
- 与队友交谈、提出计划或发起行动；
- 向 DM 发起秘密行动；
- 在需要世界或规则反馈时停下并等待 DM。

Character Agent 不可以：

- 控制 NPC、敌人、其他角色或环境；
- 宣布行动结果；
- 读取其他角色的 Prompt、秘密或记忆；
- 读取自己加入 Campaign 前的消息；
- 修改 HP、法术位、物品或其他游戏数据；
- 输出机械数值或主动申请技能检定；
- 展示内心独白、模型推理或出戏表达。

#### AI DM Agent

AI DM Agent 是真人 DM 的叙事助手，可以：

- 在 Campaign 开始前只要求真人 DM 提供模组大纲，并据此生成开场内容草稿；
- 读取仅对 DM 可见的模组内容、当前场景笔记和完整 Campaign 上下文；
- 在非战斗场景中，角色发布回复后自动生成下一段 DM 回复草稿；
- 在非战斗场景中生成场景、NPC、世界反馈和剧情推进草稿；
- 根据已经完成的检定结果生成对应的叙事草稿；
- 接受真人 DM 的粗稿或一句叙事意图，结合模组、NPC 卡片和已发布对话把它写成正式的场内叙述；
- 替模组中已有的具名 NPC 说话，并补充与当前场景一致的环境细节；
- 生成简短的公开草稿，或为一个及以上指定角色生成私密草稿。

AI DM Agent 不拥有发布权或规则裁决权，不可以：

- 绕过真人 DM 直接向 Character Agent 发布消息；
- 自行投骰、修改骰子结果或擅自决定检定参数；
- 修改 HP、技能加值或其他机械状态；
- 在战斗模式中自动主持战斗；
- 把未审核草稿写入消息历史、角色上下文、摘要或记忆；
- 新增模组中不存在的具名 NPC、地点或情节转折（无名的路人、侍者、围观者除外）；
- 隐瞒自己在真人 DM 原话之外添加的内容。

### 1.2 目标用户与平台

- MVP 只供一个真人 DM 使用；
- 不做登录和注册；
- 主要运行在本地电脑；
- 只保证桌面浏览器体验；
- 同一时间全站只允许一个 ACTIVE Campaign。

### 1.3 成功标准

以 5 名角色为主要测试规模、最多 6 名角色时，系统能够让多个拥有独立人设、认知和长期记忆的 Character Agent，在真人 DM 主持下持续产生自然、有区别、重视伙伴关系且不越过叙事权限的互动。

## 2. 产品原则

1. DM 永远拥有最终叙事和规则裁决权。
2. Character 是全局长期实体，不和单个 Campaign 绑定。
3. 每个角色只能知道自己实际经历或被告知的内容。
4. 私密隔离必须由系统保证，不能只依赖 Prompt。
5. 跑团叙事不是回合制；AI 根据最新已发布事件持续判断。
6. 每次 Agent 响应最多生成并发布一个完整气泡。
7. AI 消息需要 DM 裁决时，所有 AI 立即停止。
8. DM 新消息优先于所有未发布 AI 候选。
9. 普通消息不允许编辑或删除，错误通过 OOC 修订。
10. AI 不写入机械状态；系统严格维护 HP、十八项技能加值和检定记录。
11. 所有角色关系使用定性记忆，不建立数值好感系统。
12. 已发布消息、角色配置、记忆和运行状态自动保存。
13. AI DM 的所有输出必须先形成可编辑草稿，经真人 DM 确认后才能发布。
14. AI DM 可以建议检定，但只有真人 DM 可以确认检定参数并触发投骰。
15. 骰子结果只作为 AI DM 的叙事输入；Character Agent 默认只看到真人 DM 审核后的叙事。
16. 一个 Campaign 只有一条连续跑团时间线，不再创建或切分多个 Session；暂停和继续不会创建新时间线。
17. AI DM 的开场草稿是可选准备流程；正式跑团中只在角色需要 DM 裁定、连续 AI 发言达到上限或全员沉默时按需自动生成草稿，不要求真人 DM 每轮手动输入提示词。
18. 真人 DM 手动输入提示词属于可选辅助功能，只生成待审核草稿，不改变自动触发和人工发布边界。
19. Character Agent 的核心连续性由 Roleplay Prompt、说话范例、动态角色关系和完整故事档案共同组成。
20. 角色关系由系统根据双方共同可知的有效故事自动生成和更新，真人 DM 不手动维护关系正文。
21. Character 与 AI DM 默认使用短回复，不复述前情或进行大段文学铺陈。
22. 每次按需触发新的 AI DM 草稿时都以最新正式消息为准；未发送的旧草稿不得阻塞或覆盖新草稿。
23. AI DM 私密草稿必须明确保存接收角色，并复用正式消息的 Recipient Snapshot 隔离规则。
24. Character Agent 与 AI DM 可以自然使用第一、第二人称代词；仅在需要消除歧义或保护陌生角色身份时使用姓名或明确称呼。
25. 角色的身份（Roleplay Prompt、说话范例、成长档案）必须作为该角色专属的系统提示词注入，不能与世界状态并列成为普通上下文字段。
26. 通用行为约束以正面描述为主；密集的否定式规则会让所有角色收敛为语气相同的中性助手。
27. 发布前校验失败不得中断跑团。同一角色最多重试一次，随后顺延到下一个候选，全部失败按全员沉默处理并给出非阻塞提示。
28. AI DM 的全部触发类型共享同一个上下文构建入口；自动草稿与真人润色的唯一差别是是否包含真人 DM 的草稿。
29. AI DM 必须逐条声明自己在真人 DM 原话之外添加了什么，使审核成本停留在扫一眼清单而非重读模组。
30. AI DM 的私密草稿在收件角色非法时不得降级为公开。

## 3. 核心功能列表

### 3.1 全局角色库

- 创建和编辑全局 Character；
- 设置角色名称和可选头像；
- 编辑 Roleplay Prompt；
- 编辑外貌与视觉表现，统一使用「固定轮廓、面部与头发、服装与装备、魔法视觉、习惯性姿态与情境变化」五段结构；没有魔法视觉的角色可以将该段留空；
- 编辑说话范例：3～5 组「情境 → 这个角色会怎么说」的示例台词；
- 编辑用词约定：叙述必须遵守的说法，优先级高于角色卡记录的内容（例如角色卡写的是弯刀，但叙述一律称作长剑）；
- 编辑行为规则：「情境 → 判断 → 行动」形式的可执行规则，一行一条；
- 编辑绝不会做的事：该角色绝不说的话、绝不做的动作；
- 上传固定模板 Excel Character Sheet；
- 解析并预览种族、职业、背景、能力、法术、装备与说明；
- 在角色卡确认前查看、修改、删除或补充自动识别的法术及无数值叙事摘要；
- 激活角色卡后独立维护角色法术书，无需重新上传 Excel；
- DM 手动设置 Max HP；
- 查看和编辑 Character Development Profile；
- 查看、添加、编辑、删除和固定长期记忆；
- 查看由共同故事自动生成的角色关系；
- 导出角色 Prompt、外貌与视觉表现、说话范例、成长档案、关系、记忆、法术书和能力快照。

角色可以参加多个 Campaign，其 Prompt、能力、Max HP、成长档案和长期记忆跨 Campaign 保留。

### 3.2 Excel Character Sheet

- 所有角色使用同一个固定模板；
- Excel 用于提供角色真实拥有的身份、能力、法术、装备和说明；
- 不从 Excel 读取当前 HP 或 Max HP；
- 不向 Agent 提供未选择能力或数据库页；
- 上传后必须先解析预览，再确认激活；
- 系统从角色卡已选择法术和“法术大全”中构建初始法术书，将规则描述转换为不含机械数值的叙事摘要；
- DM 可以在确认激活前编辑初始法术书；确认内容成为 Character Agent 的封闭法术白名单；
- 激活后 DM 可以单独新增、编辑或删除法术；修改从下一次 Character Agent 调用开始生效；
- 法术摘要保留用途、目标、限制和可观察效果，但不包含骰子、伤害值、距离数值或其他机械数字；
- 未匹配到可靠说明的法术必须由 DM 确认或补写，系统不得自行猜测效果；
- 解析失败不能覆盖原有效角色卡；
- 替换角色卡不修改 HP、消息或记忆。

### 3.3 Campaign 管理

- 创建 PREPARATION Campaign；
- 在筹备阶段选择 1～6 名角色；
- 在筹备阶段粘贴模组内容；MVP 可选支持上传 `.txt` 或 `.md` 并保存解析后的文本快照；
- 可以直接启动为全站唯一 ACTIVE Campaign，也可以先根据模组生成开场草稿并在确认发布时启动；
- ACTIVE 后允许新增角色，但不允许角色离队；
- ACTIVE Campaign 可以暂停为 PAUSED，暂停时保存全部消息和运行状态；
- 在没有其他 ACTIVE Campaign 时，可以继续 PAUSED Campaign；
- 完成 Campaign；
- 归档 PREPARATION 或 COMPLETED Campaign；
- 重置全部跑团进度；
- 永久删除 Campaign；
- 删除或重置前提供导出入口。

中途加入的角色完全不知道加入前发生的公开或私密内容，但仍保留自己从其他 Campaign 获得的真实记忆。

### 3.4 Campaign 连续运行与暂停

- 一个 Campaign 从启动到完成始终只有一条连续消息时间线；
- 产品不提供创建、结束或切换 Session 的功能；
- OPENING 草稿生成期间 Campaign 保持 PREPARATION，不初始化正式消息时间线；
- 真人可以直接启动 Campaign，原子地初始化角色 HP、消息序号和运行状态，随后自行发送第一条 DM 消息；
- 真人也可以确认发布 OPENING 草稿，在同一操作中初始化时间线并把开场作为第一条场内消息；
- 刷新、关闭网页或隔天回来不会暂停或完成 Campaign；
- DM 可以主动暂停 Campaign；
- 暂停会取消所有尚未发布的 Character Agent 候选和正在生成的 AI DM 请求；
- 暂停保留消息、HP、技能、检定历史、待裁决事项、场景笔记和已完成草稿；
- PAUSED Campaign 只读，不能发送消息、投骰或生成 AI DM 草稿；
- 继续 Campaign 后恢复同一条时间线，不重置 HP、上下文、消息序号或待裁决事项；
- 暂停时更新 DM 和各角色的滚动 Campaign 摘要，但不生成最终成长档案；
- Campaign 完成时生成最终摘要、长期记忆和角色成长档案，随后默认只读。

### 3.5 消息与可见范围

支持：

- DM 场内消息；
- DM OOC 消息；
- Character 公开消息；
- Character DM_ONLY 消息；
- PUBLIC、PRIVATE 和 DM_ONLY 可见范围；
- 发送时的可见角色快照；
- 发送时可选的点名角色列表。

规则：

- PUBLIC 只对发送时已经在 Campaign 中的角色可见；
- PRIVATE 只对 DM 选择的角色可见；
- DM_ONLY 只对发送角色与 DM 可见；
- 未收到私密消息的角色不知道该消息存在；
- 角色收到私密信息后可以自主选择是否公开；
- 消息正文发布后不允许普通编辑、删除或打断；
- DM 发送消息时可以可选地勾选这句话说给谁听；被点名的角色必须能看到该消息。

### 3.6 OOC 修订

- DM 可以发送公开或私密 OOC；
- OOC 使未发布旧候选失效；
- OOC 可以取消当前待裁决事项；
- OOC 不作为场内事件进入记忆，但纠正后的事实影响认知；
- 当前 Campaign 最新有效非 OOC 消息可以被替代；
- AI 消息由原角色根据 OOC 重新生成；
- DM 消息由 DM 在表单中提供正确版本；
- 如果角色纠正后选择沉默，原消息作废且不生成替代气泡；
- 较早事实只能从当前时点向后纠正，不回滚整段历史；
- UI、Agent Context、摘要、记忆和导出使用统一有效消息规则。

### 3.7 HP 与健康状态

- DM 查看并编辑准确 Current HP；
- Current HP 限制在 `0～Max HP`；
- 修改 Max HP 时，当前 ACTIVE Campaign 的 Current HP 自动设为新 Max HP；
- HP 修改立即保存，但不触发 AI，也不产生聊天消息；
- Agent 不读取准确数值；
- Agent 只读取自己和队友的定性状态：健康、轻伤、重伤、濒危、昏迷。

### 3.8 Character Agent

每次调用分成两部分注入。

**角色身份**（作为该角色专属的系统提示词）：

- Roleplay Prompt 原文，保留其原有的段落与标题结构；
- 外貌与视觉表现；
- 行为规则；
- 说话范例；
- 绝不会做的事；
- 用词约定；
- Character Development Profile；
- 精简后的通用边界与输出要求。

**世界状态**（作为上下文）：

- 全局角色规则；
- 与其他已认识角色的当前关系摘要及重要共同经历；
- 当前真实能力说明（从角色卡快照压缩为可读文本，剔除解析元数据与未选择内容）；
- DM 已确认的法术封闭白名单及无数值叙事摘要；
- 定性健康状态；
- 按发生时间排列的全部已结束 Campaign 角色可知故事摘要；
- 当前角色长期记忆；
- 当前 Campaign 中该角色可见的有效消息记录；
- 单独成段、置于上下文末尾的最新触发事件。

长期记忆按「固定 → 当前 Campaign → 最近」的优先级选取并受字数预算约束；当前 Campaign 消息保留最近若干条。触发事件必须从历史记录中摘出单独成段，否则角色无法判断该对哪一句反应。

每次输出：

- `SILENCE`；或
- 一个完整候选气泡；
- 可选的紧急性、点名对象、DM_ONLY 和待裁决信息；
- `action_source` 与 `source_name`：使用法术时必须声明 `SPELL` 及白名单中的准确法术名；
- 两段只供模型自己使用的判断，不入库、不展示、不进入任何消息或摘要：
  **局面判断**（现在发生了什么、别人刚才说了什么、有没有人在等回答；同时检查自己最近 3～5 次回复的动作结构、收尾方式、提问及已有答案）与
  **回应意图**（按人设与行为规则决定为什么此刻回应；可以行动、提问、回应关系或单纯表达情绪，同时检查是否与他人重复、是否在替别人作答、是否重复自己的近期表达或问题）。

「怎么想」「做什么」「怎么说」必须分开，且按这个顺序产生。三者混在一起时，语言风格会反过来主导人物行为：台词先写顺了，动机再去迁就它。三段都在同一次模型调用内完成，不额外增加调用次数与延迟。

是否需要 DM 裁决必须在写出台词之前判断。若把这个判断放在台词之后，模型面对一段已经读起来完整的回复，
会倾向于认为不需要裁决，向 NPC 提问这类情形就会被漏报。

角色人格由四层共同定义，缺一层就会退化为「同类角色的平均值」：Roleplay Prompt 描述她是谁，行为规则规定她在具体情境下怎么做，说话范例示范她怎么说，绝不清单切掉她不会做的事。其中行为规则与绝不清单对区分行为模式相近的角色最有效，因为形容词高度重叠的角色，其条件-反应对与禁忌往往完全不同。

角色消息只能包含可观察的台词、动作、姿态、神态和声音。

外貌 Prompt 是描写素材而不是每轮检查清单。只有当前动作、情绪、光线、服装变化或施法让某个外貌细节自然相关时，角色才带出一两个细节；不得完整复述外貌，不得为了展示设定插入无关描写，也不得连续多轮重复同一特征。

角色只能写自己的言行。不得写出 NPC 或其他角色尚未给出的回答、反应或态度，也不得顺着一个尚未有人回答的问题继续往下说。上下文中没有人回应，就是还没有人回应，不是角色可以自行补全的空白。

角色不得重复别人刚说过的内容：复述、改写、总结、点头附和、把对方的意思换个说法讲出来都算重复。若想表达的意思已被他人表达过，应选择沉默。角色还必须检查自己最近 3～5 次回复，不得换词复刻相同的动作结构、收尾方式、比喻或句式。

角色提问前必须确认本次真正需要的信息，并检查最近对话。若已经向同一 NPC 或同伴问过相同或近似问题，不得换一种说法再次询问。对方回答不知道、不记得、没见过、无法确认或明确拒绝时，视为该来源目前无法提供信息；角色应询问其他来源、寻找线索、改变调查方式或结束话题。只有出现明确新证据、情况发生实质变化或本次询问的是不同信息时，才能再次提及。

角色可以发表不推动剧情、没有实际诉求的情绪或关系表达。开心、惊讶、遗憾、紧张、感动、厌恶和期待等由当前事件自然触发的反应，本身就是有效回复；但不得用与场景无关的感叹填充回合。

角色只能从法术封闭白名单中选择法术，不得根据职业印象发明、改名、组合或扩大法术效果。发布前系统始终校验 `source_name`，即使通用语义 Validator 关闭也不得绕过；不在白名单中的法术会触发同角色重试一次，仍失败则跳过该候选。

Character Agent 与 AI DM 的正文都不得使用破折号；需要停顿时使用逗号、句号或省略号。

角色回复可以只有台词、只有动作、「一个动作 + 一句台词」，或者一句不带诉求的真实情绪表达；目标 80 个汉字以内，正文硬上限为 240 个字符。动作和神态是可选项，不是每轮必需内容；一句台词已经成立时直接说话。只有动作能自然体现当前情绪、态度或行动，而且最近 3～5 次回复没有使用过相同结构时才写。不得换词复刻「转身离开后回头补充」「走出几步又停下」「说罢准备离开又补一句」等同构动作，也不得使用「皱了皱眉」「叹了口气」「耸耸肩」这种放在谁身上都成立的通用动作。长度要求在提示词中只出现一次；反复强调简短会让角色收敛到最短的安全句，失去性格细节。Agent 不得为了展示记忆而逐条复述关系或故事；只有与当前情境自然相关时才体现。

Character Agent 可以在正文和裁决请求中自然使用“我、我们、你、你们、您”等第一、第二人称，例如卡莱拉可以写“我推开门，你们跟上”。需要消除指代歧义时使用角色姓名；尚未认识时仍使用系统提供的陌生人编号或明确身份称呼，不得因此知道对方真实姓名。

### 3.9 Multi-Agent Orchestrator

- 每条新消息发送给所有有权看到它的角色；
- 每个角色使用独立 Agent 调用；
- 所有角色可以独立选择沉默或回应；
- Orchestrator 根据点名、紧急性和发言公平性对候选排序；
- 点名判定依次尝试三层：消息自带的点名列表、由发布角色候选继承而来的点名对象、角色姓名匹配。仅靠姓名匹配无法识别「你们两个先进去」这类代词点名，会使优先级第一档失效并退化为轮流发言；
- 按排序逐个校验候选：失败时携带失败原因让同一角色重新生成一次，仍失败则顺延到下一个候选；
- 一次只发布一个完整消息；
- 新发布消息重新触发所有有权感知的角色，但不包括发言者本人：角色不回复自己刚说的话；
- 因此单角色战役、或只发给一名角色的私密消息，在该角色发言后不会产生后继候选，直接进入等待 DM；
- 不按照固定顺序轮流发言；
- 全员沉默时回到 IDLE；
- 两条 DM 消息之间最多连续发布 5 条 AI 消息；
- 单角色失败不影响其他角色；
- 全部候选均未通过校验时按全员沉默处理，Runtime 回到 IDLE 并显示非阻塞提示，不中断跑团；
- 全部模型调用失败时才进入 ERROR，并允许重试最新事件。

### 3.10 等待 DM 与停止

是否需要 DM 介入由角色自己在生成时声明，而不是按固定节奏停下。

需要 DM 裁决的情形：

- 角色想做的事可能失败：撬锁、翻墙、潜行、搜查、说服、威吓、回忆某段知识；
- 角色对 NPC 说话或提问，需要知道对方如何回应；
- 角色想从环境中获得信息：这是什么、上面写了什么、有没有人来过、声音从哪里来；
- 任何角色此刻并不知道、必须由世界告知的内容。

判断标准是：这句话说完之后会发生什么，是否完全由角色自己决定。

不需要 DM 裁决、可以继续自由推进的情形：

- 角色之间说话、回应、商量或争论；
- 角色表达情绪、态度、判断、打算和拒绝；
- 结果确定的小动作：站起来、走过去、递出物品、拉低兜帽。

角色向 NPC 提问后必须停下等待 DM，不得替对方作答，也不得假设对方已经回答再继续往下说。

除角色自行声明外，系统还提供一层确定性兜底：当角色在 DM 场内消息之后提出问题，且问题既未点名任何队友、正文中也没有出现队友姓名时，视为向世界提问并强制等待 DM。

该兜底刻意偏向「多等」：多等一次只是多出一份真人 DM 可以忽略的草稿，漏等则等于让 AI 替世界发言。

当最终角色消息需要 DM 裁决：

- 消息正常发布；
- 页面显示角色原话提出的等待事项；该事项在 AI DM 草稿生成成功或失败后都必须保留，不得被草稿流程清除；
- 所有 AI 停止；
- Campaign Runtime 进入 WAITING_FOR_DM；
- DM 任意新 IN_GAME 消息视为完成当前裁决并触发新判断。

DM 可以随时：

- 点击“停止 AI 自动对话”；
- 在 AI 运行中发送新消息；
- 保留已经发布的消息；
- 使所有尚未发布的旧候选失效。

### 3.11 Context、Memory 与成长

每个角色独立维护：

1. Recent Context；
2. Rolling Campaign Summary；
3. Long-Term Character Memory；
4. 自动生成的定性 Character Relationship Memory。

角色关系规则：

- 关系为两个全局 Character 之间的对称、跨 Campaign 定性记忆，不使用好感度、阵营值或情绪数值；
- 每次正式 DM 或 Character 场内消息发布后，系统根据每一对角色共同可知的完整有效故事刷新关系；
- 只有故事明确表明双方见面、交谈、互相辨认或共同参与同一事件时，才能自动建立相识关系；
- 单方 PRIVATE、DM_ONLY 或其他不可见内容不得进入双方关系摘要；
- OOC 修订后使用有效消息视图重算，已被纠正的事件不得继续影响本 Campaign 新建立的关系；
- 真人 DM 只能查看自动生成的关系，不提供手动勾选或编辑入口。

Campaign 暂停时：

- 更新 DM 滚动摘要，仅供 DM 查看；
- 更新每个角色只包含该角色可知信息的独立滚动摘要；
- 单项生成失败不阻止 Campaign 暂停，可在继续后重试。

Campaign 完成后：

- 生成最终 DM 摘要和角色独立摘要；
- 提取值得跨 Campaign 保存的长期记忆；
- 根据角色自己的记忆更新成长档案；
- 不自动改写 Roleplay Prompt；
- 不建立数值关系系统。

### 3.12 导出与删除

Campaign 导出包含：

- 最终有效公开、私密和 DM_ONLY 对话；
- 角色和 DM 摘要；
- 参与角色当前 Roleplay Prompt；
- 来源属于该 Campaign 的角色记忆；
- 消息发送者和可见范围。

角色导出包含：

- Roleplay Prompt；
- 说话范例；
- Character Development Profile；
- 自动生成的角色关系与共同经历；
- 全部长期记忆及来源；
- 当前能力、法术、装备和背包快照。

Campaign 重置或永久删除时，必须移除其来源记忆并重建受影响角色成长档案；不得影响角色来自其他 Campaign 或 DM 手动添加的记忆。

### 3.13 AI DM Agent 与审核草稿

- PREPARATION Campaign 面向 AI DM 的必填资料只有模组大纲；战役名称只用于产品内识别，不属于 AI DM Prompt；
- 模组内容是 AI DM 的长期权威剧情来源，不作为正式消息发送给 Character Agent，也不进入角色 Context；
- 优先级固定为：模组内容 > 场景笔记 > 最近对话；三者冲突时以模组为准；
- 筹备阶段可以从模组正文一次性提取具名 NPC 卡片（详见 3.15）；
- Campaign 正式开始前，真人 DM 可以选择“生成开场草稿”，由 AI DM 根据模组大纲、阵容和已有跨 Campaign 关系生成 `OPENING` 草稿；
- 真人 DM 可以编辑、重新生成、丢弃或跳过开场草稿；点击“确认开团并发送”会启动 Campaign 并把草稿转化为第一条正式 DM 消息，点击“启动战役”则直接进入 ACTIVE，之后由真人 DM 自行发送第一条消息；
- ACTIVE Campaign 支持“探索/叙事”和“战斗”模式，由真人 DM 控制；
- AI DM 只在非战斗模式中生成叙事草稿；技能检定参数仍由真人 DM 发起和确认；
- 非战斗模式下，AI DM 草稿在场面确实需要真人 DM 介入时才自动触发，而不是每条角色回复后都触发。触发时机为：角色发言需要 DM 裁定、连续 AI 发言达到上限、或全员沉默使场面停住；
- 自动草稿生成期间和草稿等待审核期间，不继续自动发布新的 Character Agent 消息，避免 AI DM 与角色链式对话越过真人 DM；
- 角色之间的纯互动不触发草稿，也不需要真人 DM 介入；
- 角色请求 DM 裁决、已确认的检定结果也可以触发相应草稿；
- 真人 DM 可以用最简单的语言写下想回什么，或直接给出一段回复初稿；AI DM 保留其全部意图与信息，补上氛围、NPC 语气和画面，并生成 `MANUAL_ASSIST` 草稿；
- 润色支持三种模式：`POLISH` 只改文笔、`EXPAND` 在不新增事实的前提下扩写细节、`REWRITE` 可重组结构但信息必须一致；
- 手动辅助是可选项；不填写提示词时，开场与角色回复后的自动草稿流程仍须完整工作；
- 自动草稿与手动润色共享同一份上下文：模组、场景笔记、NPC 卡片、带发送者姓名与可见范围的最近对话、最新角色回复、角色相识状态与检定结果。两者唯一的差别是是否包含真人 DM 的草稿；
- 草稿可以在不清空原始粗稿的情况下重新生成；
- AI DM 结构化输出包含草稿正文、`PUBLIC/PRIVATE` 可见范围、私密接收角色 ID，以及一份「AI 自行添加内容」清单；
- 该清单逐条列出 AI 在真人 DM 原话之外补充了什么，并在草稿下方单独展示；完全照真人 DM 的意思写时为空；
- 叙事草稿支持编辑、重新生成、丢弃、公开或私密发送；
- AI DM 草稿通常为 1～4 句，建议不超过 300 个汉字，模型输出硬上限为 800 个字符；真人编辑后的正文最多 1200 个字符；
- AI DM 生成的叙述和台词允许自然使用第一、第二人称代词；需要消除歧义时使用角色姓名，未知人物使用“守卫”“陌生人”等明确身份称呼；
- PRIVATE 草稿必须包含 1～6 个当前 Campaign 角色 ID；发布时只为这些角色创建 Message Recipient Snapshot；
- 模型给出的收件角色 ID 不合法时直接剔除；若一个都不剩，草稿仍保持 PRIVATE 且收件人为空，由真人 DM 补选。任何情况下都不得因为 ID 错误而把私密草稿降级为公开；
- 草稿保存在服务端，页面刷新后仍可恢复；
- 每当创建新的 `CHARACTER_REPLY` 草稿时，所有未发送的非开场 `GENERATING/READY/DRAFT` 草稿先标记为 `STALE`，再基于最新回复创建新草稿；同一条触发消息保持幂等，不会重复建稿；
- 较早的并发模型请求即使迟到，也只能保留为 `STALE`，不得覆盖较新的草稿；
- 草稿生成不得修改 Runtime 状态。此时的 `WAITING_FOR_DM` 承载的是角色原话提出的裁定请求，草稿成功或失败都必须原样保留，否则真人 DM 会看不到自己要回答的那个问题；
- 点击丢弃必须把草稿持久化为 `DISCARDED`，刷新页面后不得再次成为当前草稿；
- 真人 DM 使用手动辅助时，系统先停止所有尚未发布的 Character Agent 候选，并以最新正式消息水位构建上下文；
- 上下文已变化的草稿禁止直接发送，必须重新生成或复制到人工输入框；
- 只有真人 DM 点击发送后，草稿才转化为正式 DM 消息并触发 Character Agent；
- 草稿本身不进入消息时间线、角色上下文、摘要、记忆或导出中的正式对话记录；
- 战斗模式不自动生成 AI DM 草稿，角色回复也不触发 AI DM；战斗过程由真人 DM 主持；
- 从战斗模式返回探索/叙事模式后，只处理切换后的新角色回复，不自动补生成战斗期间遗漏的草稿。

### 3.14 十八项技能与 D20 检定

第一版只记录以下十八项标准技能的最终检定加值：

- 运动、体操、巧手、隐匿；
- 奥秘、历史、调查、自然、宗教；
- 驯兽、洞悉、医疗、察觉、生存；
- 欺瞒、威吓、表演、游说。

第一版不记录属性检定、豁免、先攻、攻击骰、伤害骰或自定义技能。

- DM 可以为每个角色查看和维护十八项技能加值；
- 一次检定必须选择角色和技能，并支持普通、优势、劣势；
- DC 和检定原因可选；填写 DC 时由系统计算是否达到 DC；
- AI DM 不生成或确认检定参数，也不能自行触发投骰；
- 真人 DM 必须确认或修改全部检定参数，并点击“确认并投掷”后才生成随机结果；
- 真人 DM 也可以不经过 AI DM，直接手动发起检定；
- 投骰由后端完成并记录所有原始 D20、采用值、技能加值快照、总值、DC、结果和时间；
- 已发生的投骰不能修改或删除，误投可以标记为作废；
- 有 DC 时，AI DM 必须遵守系统计算的成功或失败结果，只负责生成对应叙事；
- 没有 DC 时，真人 DM 必须选择成功、失败或部分成功后，才能让 AI DM 生成结果叙事；
- AI DM 不得自行改变或补充真人 DM 的检定裁决；
- 检定结果默认只对真人 DM 和 AI DM 可见，不直接成为 Character Agent 可见的消息；
- AI DM 根据结果生成的叙事仍须由真人 DM 编辑并确认发送；
- 修改角色技能加值不会改变历史投骰保存的加值快照。

### 3.15 模组 NPC 卡片

- 筹备阶段可以从模组正文一次性提取具名 NPC，生成卡片供 AI DM 使用；
- 每张卡片包含姓名、身份、个性特点、理想、牵绊、缺陷、知道什么、想要什么和说话方式；
- 提取只能使用模组原文明确写出的内容；原文没有写的字段留空，不得推测或补写；
- 真人 DM 可以查看、编辑、新增和删除卡片；
- 被手动编辑过的卡片不会被重新提取覆盖；重新提取只刷新自动生成的卡片，并清理模组中已不再出现的自动卡片；
- AI DM 替 NPC 说话时必须符合卡片中写明的性格；卡片中没有的具名 NPC 不得凭空创造；
- NPC 卡片只进入 AI DM Context，不进入任何 Character Context。

## 4. 核心用户流程

### 4.1 创建角色

```text
进入全局角色库
→ 创建角色
→ 输入名称
→ 上传可选头像
→ 填写 Roleplay Prompt
→ 上传固定模板 Excel
→ 输入 Max HP
→ 查看解析预览
→ 确认创建
```

### 4.2 筹备 Campaign

```text
创建 Campaign
→ 输入用于识别的 Campaign 名称
→ 输入唯一必需的 AI DM 资料：模组大纲
→ 从全局角色库选择 1～6 名角色
→ 检查角色配置完整性
→ 确认当前没有其他 ACTIVE Campaign
→ 可选：请求 AI DM 生成开场草稿
```

### 4.3 启动 Campaign

```text
PREPARATION Campaign 检查阵容、角色配置和唯一 ACTIVE 约束
→ 路径 A：真人 DM 点击“启动战役”
→ 原子地初始化唯一连续时间线和角色 HP
→ Campaign 变为 ACTIVE，等待真人 DM 发送第一条消息

或

→ 路径 B：AI DM 读取模组大纲、阵容和已有角色关系，生成 OPENING 草稿
→ 真人 DM 编辑、重新生成或丢弃草稿
→ 真人 DM 点击“确认开团并发送”
→ 原子地初始化唯一连续时间线和角色 HP
→ Campaign 变为 ACTIVE
→ 开场草稿成为第一条正式 DM 消息
→ Character Agent 根据开场内容回复
```

### 4.4 暂停和继续 Campaign

```text
DM 点击暂停 Campaign
→ 取消所有未发布的 Character Agent 候选和正在生成的 AI DM 请求
→ 保存消息、HP、检定、场景、草稿和待裁决事项
→ 更新滚动摘要
→ Campaign 变为 PAUSED 并保持只读
→ DM 在没有其他 ACTIVE Campaign 时点击继续
→ 恢复同一条跑团时间线和运行状态
```

### 4.5 DM 消息触发角色

```text
DM 发送消息
→ 保存消息和接收者快照
→ 旧候选失效
→ 有权看到消息的角色独立判断
→ Orchestrator 选择一个候选
→ 最终候选通过发布前检查
→ 一次性发布完整气泡
→ 新气泡成为下一事件
→ 非战斗模式自动暂停后续 Character 发布并触发 AI DM 回复草稿
```

### 4.6 等待 DM

```text
角色发起需要外部结果的行动
→ 发布角色消息
→ 页面显示待裁决事项
→ Character Runtime 停止
→ AI DM 自动生成简短叙事草稿
→ DM 审核并发送 IN_GAME 裁决
→ 清除等待状态
→ 角色根据最新事实重新判断
```

### 4.7 AI DM 生成并发布叙事草稿

```text
非战斗模式下 Character Agent 发布正式回复
→ 把尚未发送的旧非开场草稿标记为 STALE
→ 系统自动创建 CHARACTER_REPLY 草稿生成任务
→ AI DM 读取模组内容、场景笔记、滚动摘要、已发布对话和最新角色回复
→ 生成简短的 PUBLIC 或 PRIVATE 叙事草稿
→ 真人 DM 查看并编辑正文、公开/私密范围和私密收件角色
→ 真人 DM 点击公开发送或私密发送
→ 草稿按指定 Recipient Snapshot 转化为正式 DM 消息
→ Character Agent 根据正式消息继续判断

可选分支：真人 DM 用最简单的语言写下想回什么
→ 选择只改文笔 / 扩写细节 / 重写结构
→ AI DM 读取与自动草稿完全相同的上下文，外加这段粗稿
→ 生成 MANUAL_ASSIST 草稿，并列出自己额外添加了什么
→ 真人 DM 编辑、重新生成或直接发送
```

### 4.8 真人 DM 发起技能检定

```text
真人 DM 判断行动需要检定
→ 真人 DM 确认角色、技能、DC 和优势状态
→ 真人 DM 点击“确认并投掷”
→ 后端生成并保存 D20 结果
→ AI DM 根据确定的检定结果生成叙事草稿
→ 真人 DM 编辑并确认发送
```

### 4.9 OOC 修订最新 AI 消息

```text
DM 点击“纠正此消息”
→ 输入 OOC
→ 原消息和旧候选失效
→ 原 Character 根据纠正重新处理
→ 新版本通过校验
→ 替代原气泡并显示 OOC 修订标志
→ 相关角色根据有效版本继续
```

### 4.10 完成 Campaign

```text
DM 点击结束 Campaign
→ 取消所有未发布输出
→ 保存最终 HP 和检定历史
→ 生成最终 DM 摘要和角色独立摘要
→ 提取长期记忆
→ Campaign 变为 COMPLETED
→ 更新参与角色成长档案
→ Campaign 默认只读
```

### 4.11 重置或永久删除 Campaign

```text
选择重置或永久删除
→ 可选先导出
→ 输入 Campaign 名称二次确认
→ 停止未完成 Agent Run
→ 删除消息、检定、草稿、摘要和来源记忆
→ 重建受影响角色成长档案
→ 重置时保留 Campaign 和阵容
→ 永久删除时移除 Campaign 本身
```

## 5. MVP 暂不包含

- 多用户、登录、权限和云端账号；
- 多个同时 ACTIVE 的 Campaign；
- 一个 Campaign 内的多个 Session 或分段时间线；
- ACTIVE Campaign 角色离队；
- AI DM 自动发布、独立裁决或战斗主持；
- 独立 NPC Agent 或不受模组约束的世界自动生成；
- 移动端；
- 消息全文搜索；
- Campaign 导入恢复；
- 严格法术位、物品数量和能力次数追踪；
- 属性检定、豁免、先攻、攻击骰、伤害骰和完整规则引擎；
- 数值关系、好感度或情绪系统；
- 语音、图片生成和聊天附件；
- Docker 和分布式部署。

## 6. 验收重点

MVP 至少必须证明：

- 新角色无法读取加入前消息；
- 私密消息不会进入无权角色的 Context、Summary 或 Memory；
- DM Summary 永不进入 Character Agent；
- AI DM 草稿未经真人 DM 确认绝不成为正式消息；
- PREPARATION Campaign 可以直接启动，也可以根据模组生成开场草稿；生成或编辑草稿不会产生正式消息或触发 Character Agent；
- 直接启动会初始化连续时间线并等待真人 DM 的第一条消息；发布开场草稿则会启动 Campaign，并将该草稿作为第一条正式 DM 消息；
- 非战斗模式下，仅当角色需要 DM 裁定、连续 AI 发言达到上限或全员沉默时，才自动触发一份基于模组和最新对话的 AI DM 草稿；
- AI DM 草稿等待审核时，不会继续自动发布新的 Character Agent 消息；
- 不输入手动提示词时，可选的开场草稿与跑团中的按需自动草稿流程仍然可用；
- DM 可以可选地写下一句粗稿，让 AI DM 只改文笔、扩写细节或重写结构，且与自动草稿使用同一份上下文；
- 战斗模式中的 Character 回复不会自动触发 AI DM 草稿；
- 新正式消息产生后，旧 AI DM 草稿不会被无提示发送；
- 新 Character 回复即使发生在旧草稿未发送或未使用时，也会生成新的草稿，旧草稿变为 STALE；
- 迟到的旧草稿生成结果不会覆盖基于更新角色回复的新草稿；
- AI DM 草稿生成完成后 Runtime 不会继续卡在上一次的等待请求；
- 丢弃的草稿刷新后不会重新出现；
- AI DM 和 Character Agent 的模型输出分别满足 800 与 240 字符硬上限；
- 角色的 Roleplay Prompt 与说话范例进入该角色专属的系统提示词，且原文结构未被破坏；
- 不同角色在同一情境下的回复在语气与用词上可以明显区分；
- 触发消息在角色上下文中单独成段，且位于末尾；
- DM 用代词点名时，被点名角色仍然获得优先发言权；
- 单个候选未通过发布前校验时不会中断跑团：同角色重试一次后顺延到下一候选，全部失败按沉默处理；
- 含普通数字的台词（如「我数到三」）不被误判为机械数值；
- AI DM 的上下文中每条对话都带有发送者姓名与可见范围；
- 自动草稿与手动润色使用同一份上下文，手动润色不会缺少对话历史；
- AI DM 替 NPC 说话时符合 NPC 卡片；卡片外的具名 NPC 不会被凭空创造；
- AI DM 逐条声明了自己在真人 DM 原话之外添加的内容；
- 收件角色 ID 不合法时，私密草稿不会被降级为公开；
- 角色的用词约定优先于角色卡：角色卡记录为弯刀时，叙述仍称其为长剑；
- 角色不会复述、改写或附和其他角色刚说过的内容；
- Character Agent 与 AI DM 的正文都不出现破折号；
- 三名行为模式相近的角色（例如同为克制型）在同一情境下的神态与动作可以明显区分；
- AI DM 和 Character Agent 生成的场内正文可以自然使用第一、第二人称，并继续遵守陌生角色身份隔离；
- AI DM PRIVATE 草稿只会发布给所选 Campaign 角色，其他角色的 Context、Summary 和 Memory 均不可见；
- 角色关系无需 DM 输入，会根据双方共同可知的有效故事实时更新；
- 单方私密故事不会进入双方关系，OOC 后关系只使用纠正后的有效故事；
- AI DM 不能自行投骰，只有真人 DM 确认后才能生成结果；
- 第一版技能数据严格限制为十八项标准技能加值；
- 角色卡确认前可以编辑自动识别的法术书，激活后也可以独立新增、编辑或删除法术；
- Character Agent 使用法术时只能声明法术白名单中的准确名称，白名单外法术不会被发布；
- 原始角色卡快照中的未确认法术不会绕过 DM 已确认的法术书进入 Character Agent；
- 骰子原始结果不会直接进入 Character Agent 上下文；
- AI DM 根据有 DC 的检定生成叙事时不得改变系统确定的成功或失败；
- 多个角色可以独立判断，但一次只发布一个气泡；
- 需要裁决后没有 AI 继续说话；
- 角色之间的纯互动可以连续进行，不会每句话都停下来等 DM；
- 角色一旦触及自身视角之外的事物（行动成败、NPC 反应、环境信息），必定停下等待 DM；
- 角色不会回复自己刚发布的消息；单角色场景下发言后立即等待 DM；
- 角色不会写出 NPC 或其他角色尚未给出的回答；
- DM Stop 或新消息能可靠阻止旧候选发布；
- OOC 后所有有效视图不再使用错误版本；
- Campaign 删除后对应记忆不再进入角色 Context；
- 页面刷新后仍能恢复当前 Campaign 的运行状态和已发布消息；
- Campaign 暂停并继续后仍恢复同一消息时间线、HP、检定和待裁决事项；
- 5 名角色的主要运行规模可接受，最大支持 6 名角色。
